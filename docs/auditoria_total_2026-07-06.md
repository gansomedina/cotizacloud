# Auditoría total CotizaCloud — 6 julio 2026

Auditoría de TODO el sistema con Fable 5. 8 auditores de dominio; cada hallazgo pasó por un verificador adversarial que intentó refutarlo leyendo el código. Solo aparece lo **confirmado**.

Cada hallazgo trae **evidencia** (código citado), **impacto** (escenario de negocio) y **fix**. Los marcados _(verificado inline)_ los confirmó Fable 5 leyendo el código cuando el límite de sesión cortó su verificador.

**Conteo:** 🔴 7 críticos · 🟠 20 altos · 🟡 49 medios · ⚪ 13 bajos.

---

## Seguridad

### 🔴 CRÍTICO — `api/quote_action.php:27`
**quote_action.php: IDOR sin token — acepta/rechaza y crea ventas de CUALQUIER cotización por id secuencial**

- **Evidencia:** El endpoint público (Router.php:86 `self::post('/api/quote-action', ... load_api('quote_action'))`, sin Auth) sólo lee `$cot_id = (int)$body['cotizacion_id']` (línea 14) y carga `SELECT ... FROM cotizaciones WHERE id = ? AND empresa_id = ?` con `[$cot_id, EMPRESA_ID]` (líneas 27-30). NO verifica ningún token/secreto ligado al destinatario. Con `accion='aceptar'` marca estado='aceptada', congela snapshot y hace INSERT en `ventas` (líneas 117-178); con 'rechazar' marca 'rechazada' (líneas 261-269). No hay CSRF ni rate limit.
- **Impacto:** Cualquiera que conozca el host de la empresa (empresa.cotiza.cloud) puede iterar cotizacion_id=1,2,3… y aceptar o rechazar en masa cotizaciones ajenas: crea ventas falsas con folio VTA, marca cotizaciones como aceptada/rechazada, dispara push y emails al dueño, y ensucia estados/embudo. Destruye la confianza del producto: el dueño ve 'aceptada' cotizaciones que el cliente nunca tocó y ventas fantasma en su cartera.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código real. La ruta Router.php:86 está dentro del bloque IS_SUBDOMAIN sin Auth y load_api() (281-288) solo hace require, sin login/CSRF. quote_action.php lee $cot_id del body (línea 14) y carga con WHERE id=? AND empresa_id=? usando [$cot_id, EMPRESA_ID] (27-30); EMPRESA_ID viene del host del subdominio, NO de ningún secreto ligado al destinatario. No hay token, ni csrf_check, ni rate limit en todo el archivo. Únicos guardas: no suspendida y estado en [enviada,vista,aceptada]. 'aceptar' (116-178) marca estado='aceptada', hace snapshot e INSERT real en ventas con folio VTA (solo exige nombre no vacío, línea 53) y dispara push+email; 'rechazar' (261-269) marca 'rechazada'. Escenario mínimo: POST no autenticado a https://<empresa>.cotiza.cloud/api/quote-action con {"cotizacion_id":N,"accion":"aceptar","nombre":"x"} iterando N secuencial acepta en masa cotizaciones ajenas y crea ventas fantasma; 'rechazar' ni siquiera requiere nombre. Las referencias de línea y la cadena de explotación del hallazgo son exactas. Severidad critico: no autenticado, trivialmente scripteable, muta registros financieros y corrompe el embudo/estado, destruyendo la confianza del producto.
- **Fix:** Exigir el `token` (32 hex) de la cotización en el body y validarlo: `WHERE id=? AND empresa_id=? AND token=?`. El token ya existe (cotizaciones.token) y es no-adivinable; el slug público ya lo tiene disponible para pasarlo. Sumar rate limit por IP.

### 🔴 CRÍTICO — `respaldoconfig.php + vapid_private.pem (git)` _(verificado inline)_
- **Evidencia:** Ambos archivos versionados (git ls-files los lista). respaldoconfig.php:20 define(DB_PASS, getenv(DB_PASS) ?: 'Jalfonso234') y :23 APP_SECRET fallback 'cambiar-en-produccion-32chars'. vapid_private.pem empieza con -----BEGIN PRIVATE KEY----- (llave real).
- **Impacto:** La contraseña real de producción de la BD y la llave privada de Web Push están en el historial de git. Cualquiera con acceso al repo tiene la BD completa y puede firmar push falsas a los clientes. El fallback hardcodeado ES el secreto real, no un placeholder.
- **Fix:** Rotar YA la contraseña de la BD y regenerar el par VAPID en el servidor. git rm --cached ambos, .gitignore, secretos solo en env del server. (Requiere tu acción en cPanel — no puedo rotar credenciales del server.)

### 🔴 CRÍTICO — `api/mp_return.php:12` _(verificado inline)_
- **Evidencia:** empresa_id se lee de $_GET (línea 12), no de la sesión. Sin Auth::id(). external_reference del pago (que trae la empresa real) NO se compara contra ese empresa_id. El UNIQUE de mp_payment_id solo evita el INSERT duplicado en el log (119); la activación (UPDATE empresas, 133) corre SIEMPRE, y 82 usa plan_vence futuro como base → cada replay suma +30/365 días.
- **Impacto:** Un atacante con UN payment_id aprobado (el propio sirve) llama /api/mp/return?empresa_id=X&payment_id=Y sin loguearse: activa el plan en cualquier empresa, y re-llamando la misma URL extiende su plan +30 días por hit sin volver a pagar.
- **Fix:** Exigir Auth::id() y tomar empresa_id de la sesión; validar external_reference del pago == esa empresa; hacer idempotente la ACTIVACIÓN por mp_payment_id (si ya está registrado, no volver a extender plan_vence).

### 🔴 CRÍTICO — `migrations/add_suscripciones.sql:10` _(verificado inline)_
- **Evidencia:** add_suscripciones.sql:10 crea suscripciones.plan ENUM('pro','business'). add_plan_lite.sql amplió empresas.plan a 'lite' pero NO tocó suscripciones.plan.
- **Impacto:** Latente hasta lanzar Lite: al pagar el plan Lite por MP, el INSERT en suscripciones con plan='lite' falla (estricto) o se trunca a '' (no estricto). El cliente paga y su suscripción no queda registrada bien.
- **Fix:** ALTER TABLE suscripciones MODIFY plan ENUM('lite','pro','business') NOT NULL antes de vender Lite.

### 🟡 MEDIO — `core/Helpers.php:131` _(verificado inline)_
- **Evidencia:** e_html usa strip_tags con lista blanca que incluye <a>, <span>, <div>. strip_tags NO elimina atributos de los tags permitidos.
- **Impacto:** XSS almacenado: dato del cliente con <a href='javascript:...'> o <span onmouseover='...'> renderizado con e_html en el slug ejecuta script en el navegador de quien vea la cotización.
- **Fix:** Sanitizar atributos (whitelist, o quitar on*/javascript: por regex), o quitar <a>,<span>,<div> de la lista.

### 🟡 MEDIO — `modules/config/ip_interna.php:8` _(verificado inline)_
- **Evidencia:** Auth::requerir_admin() presente pero NO csrf_check(). Mismo patrón en calibrar_radar.php y logo.php.
- **Impacto:** CSRF: un sitio malicioso hace que un admin logueado agregue/borre IPs o dispare recalibración sin querer. Impacto acotado, pero hueco de patrón a cerrar en todos los POST de config.
- **Fix:** Agregar csrf_check() al inicio de los POST de config.

### 🟡 MEDIO — `modules/ventas/agregar_extra.php:32`
**Endpoints de extras acotados sólo por empresa: cualquier asesor con permiso edita ventas de otros asesores**

- **Evidencia:** agregar_extra.php y eliminar_extra.php cargan `SELECT * FROM ventas WHERE id=? AND empresa_id=?` (línea 32/27) y sólo checan `Auth::es_admin() || Auth::puede('agregar_extras')` (línea 14). NO comparan usuario_id/vendedor_id de la venta con Auth::id(). El pendiente documentado pedía ownership check.
- **Impacto:** Un asesor con permiso 'agregar_extras' puede agregar/eliminar líneas extra y alterar el total/saldo de ventas de OTROS asesores de la misma empresa (no cruza tenants, sí cruza vendedores). Modifica dinero cobrable de ventas ajenas dentro del negocio.
- **Escenario que lo dispara (verificado):** VALIDADO EN CÓDIGO. (a) Punto de inserción existe: agregar_extra.php:32 y eliminar_extra.php:27 cargan la venta con `SELECT * FROM ventas WHERE id=? AND empresa_id=?` (solo tenant), y la línea 14 de ambos solo checa `Auth::es_admin() || Auth::puede('agregar_extras')` — cero comparación contra usuario_id/vendedor_id. (b) No duplica nada: el sistema YA tiene el patrón de ownership en otros lados y estos dos endpoints son la excepción — costos/eliminar_gasto.php:26 hace exactamente el check propuesto (`!ver_todas_ventas && usuario_id !== Auth::id() && vendedor_id !== Auth::id()`), ventas/acciones.php:142 lo hace para la acción 'notas', y lista.php:24 filtra la visibilidad por ver_todas_ventas. La incoherencia es flagrante: editar NOTAS de una venta ajena está bloqueado, pero alterar su TOTAL/SALDO (dinero) no. Además el asesor ni siquiera ve esas ventas en su lista (filtro solo_propias) pero puede modificarlas vía API directa. El fix es ~6 líneas copiando el patrón existente — esfuerzo declarado realista. (c) Valor de negocio real: en Business multi-asesor, un vendedor con permiso agregar_extras puede inflar/vaciar extras de ventas rivales, alterando saldo cobrable y de paso las métricas del Termómetro (pen_sin_pago, ticket_promedio, bonus) que son el diferenciador del producto. SEVERIDAD ajustada a MEDIO (no alto): es intra-tenant, requiere un permiso que el admin otorga explícitamente (insider ya confiado), solo aplica a plan Business, y VentaLog registra Auth::id() en cada operación (audit trail disuasorio y rastreable). No es explotable por externos ni cruza empresas. Prioridad: incluir en el próximo batch de seguridad — barato de arreglar y cierra el pendiente #7 de la auditoría documentada (ALTOS) que el propio proyecto dejó abierto.
- **Fix:** Añadir, cuando no es admin, el filtro de propiedad: rechazar si `venta.usuario_id != Auth::id() && venta.vendedor_id != Auth::id()` salvo permiso ver_todas_ventas (como ya hace eliminar_gasto.php).


---

## Cotizaciones

### 🔴 CRÍTICO — `public/cotizacion.php:1817`
**El slug público sobrescribe el Total al cargar: excluye extras y borra el cupón aplicado por el asesor**

- **Evidencia:** Línea 1817: `calc();` corre al cargar la página. calc() (líneas 1361-1386) reconstruye el total desde `const SUB = <?= (float)$subtotal ?>` (línea 1273), donde $subtotal solo suma líneas NO extra (línea 99: `array_sum(array_column($lineas,'subtotal'))` tras filtrar es_extra en línea 80). calc() nunca suma $subtotal_extras ni resta $cupon_monto_guardado; además con `applied=null` ejecuta `else cr.style.display='none'` (línea 1378), ocultando la fila del cupón que PHP había renderizado (líneas 892-896). El total PHP correcto de la línea 906 (`$total_base` con extras y cupón) es reemplazado en <1s.
- **Impacto:** Cualquier cotización con extras o con cupón aplicado por el asesor muestra al cliente un Total distinto al real. Ej: artículos $10,000 + extra $2,000 + IVA suma 16%: PHP renderiza $13,600, calc() lo sobrescribe a $11,600 (sin extra), y al aceptar quote_action cobra $13,920 (suma de TODAS las líneas × 1.16). El cliente acepta un monto y la venta se crea con otro $2,320 mayor — destruye la confianza que es el activo #1.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. Escenario mínimo: cotización en estado 'enviada' con 1 línea es_extra=1 (o con cupón aplicado por el asesor en el editor — guardar.php:150 guarda cupon_monto), impuesto_modo='suma'. Al abrir el slug, PHP renderiza #tTot con $total_base correcto (línea 906: base + impuesto + $subtotal_extras), pero calc() en línea 1817 corre incondicionalmente y lo sobreescribe usando SUB (línea 1273 = $subtotal de línea 99, que excluye extras por el filtro de línea 80), sin sumar extras ni restar cupon_monto_guardado; además con applied=null oculta la fila del cupón (línea 1378) que PHP pintó en 892-896. El ejemplo numérico cuadra: PHP $13,600 → calc() muestra $11,600 → al aceptar, quote_action.php:74 suma TODAS las líneas (sin filtrar es_extra) y guarda $13,920 (línea 111, ×1.16). El cliente ve un total y la venta se crea con otro mayor. Verifiqué posibles refutaciones: no existe constante JS de extras, no hay gate por estado, no hay resincronización con el total del servidor, y el modal de aceptación (openM 1390-1401) reproduce los mismos valores erróneos. Solo afecta cotizaciones con extras (Business) o con cupón pre-aplicado; sin esos elementos calc() coincide con PHP.
- **Fix:** En calc(): partir de SUB, aplicar descuentos, sumar extras al final (const EXTRAS = <?= (float)$subtotal_extras ?>) y restar CUPON_GUARDADO = <?= (float)$cupon_monto_guardado ?>; no ocultar #tCR cuando hay cupón guardado.

### 🔴 CRÍTICO — `modules/cotizaciones/guardar.php:128`
**guardar.php y quote_action.php aplican descuentos e impuesto TAMBIÉN sobre los extras; el editor y el slug los tratan aparte**

- **Evidencia:** guardar.php líneas 111-128 suman TODAS las líneas (incluidos es_extra=1) en `$subtotal += $sub_linea;` y luego aplican cupón, descuento auto e impuesto sobre ese total (148-159). El editor JS (ver.php:941-950) hace lo contrario: `let base=subRegular` (descuentos solo sobre regulares), `const total=totalBase+subExtras` (extras sin IVA ni descuento) — comentario explícito línea 942 'Descuentos/cupones solo aplican sobre artículos regulares'. El slug PHP (cotizacion.php:99-115) usa la misma convención que el editor. quote_action.php:73-75 hereda el error: `SELECT COALESCE(SUM(subtotal),0) FROM cotizacion_lineas` sin filtrar es_extra.
- **Impacto:** El total que el asesor VE en pantalla ($13,600 en el ejemplo de $10,000 + extra $2,000 + IVA 16%) difiere del total que guardar.php ALMACENA en cotizaciones.total ($13,920) — que es el que usan lista, dashboard, reportes, convertir.php y quote_action al aceptar. Toda cotización Business con extras + IVA suma o descuento % tiene dos totales distintos en el sistema.
- **Escenario que lo dispara (verificado):** Confirmado en código real. Escenario mínimo: cotización con línea regular $10,000 + extra $2,000 (es_extra=1) en empresa con impuesto_modo='suma' 16%. El editor (ver.php:942-950, base=subRegular, total=totalBase+subExtras) y el slug público (cotizacion.php:80,99,115, filtra es_extra antes de calcular base/impuesto) muestran $13,600. guardar.php suma TODAS las líneas en $subtotal (línea 128 sin filtrar es_extra) y aplica cupón (150), descuento auto (151) e impuesto (157) sobre esa base → almacena total=$13,920. quote_action.php:73-75 hace SUM(subtotal) sin filtro es_extra y al aceptar sobreescribe cotizaciones.total con el mismo cálculo errado. Resultado: el total aceptado/almacenado (usado por lista, reportes y conversión a venta) difiere del que el cliente vio y aceptó en el slug. Basta un extra + IVA suma (o cupón/descuento %) para dispararlo.
- **Fix:** En guardar.php y quote_action.php separar subRegular/subExtras (WHERE es_extra=0 en el SUM), aplicar cupón+descuento+impuesto solo a subRegular y sumar extras al final, replicando exactamente calcularTotales() del editor.

### 🟠 ALTO — `api/quote_action.php:80`
**Al aceptar, quote_action ignora el cupón guardado en la cotización: el descuento otorgado por el asesor se evapora**

- **Evidencia:** Línea 50: `$cupon_codigo = trim($body['cupon_codigo'] ?? '')` — solo se considera el cupón que el CLIENTE teclea en el slug. Líneas 80-94 revalidan únicamente ese código; nunca se lee cotizaciones.cupon_codigo/cupon_monto (el que el asesor aplicó desde el editor via guardar.php:78-89). Línea 117-125: `UPDATE cotizaciones SET ... total = ?` sobrescribe el total con `$total_guardar` recalculado SIN el cupón guardado, y la venta (líneas 156-177) se crea con ese total.
- **Impacto:** Asesor aplica cupón 10% en el editor: cotización guardada con total $9,000. El cliente abre el slug (calc() además le oculta el cupón) y acepta: quote_action recalcula $10,000, sobrescribe cotizaciones.total=$10,000 y crea la venta por $10,000. El descuento pactado desaparece, cupon_monto queda >0 en la fila con un total que ya no lo refleja, y el cliente reclama al recibir el cobro completo.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. Escenario mínimo: (1) asesor aplica cupón en el editor → guardar.php:77-89,150 persiste cupon_codigo/cupon_monto y total con descuento; (2) cliente abre el slug: aunque PHP renderiza el cupón (cotizacion.php:892-896), calc() corre al cargar (línea 1817) con applied=null (línea 1292, nunca se inicializa desde $cot['cupon_codigo']) y oculta la fila del cupón sobrescribiendo #tTot sin descuento; (3) cliente acepta sin re-teclear el código → doAcc() (línea 1421) envía cupon_codigo:null; (4) quote_action.php:80 if($cupon_codigo) es false → cupon_amt_srv=0, nunca lee cotizaciones.cupon_codigo/cupon_monto, línea 117-125 sobrescribe total sin el cupón y línea 156-177 crea la venta al precio completo con cupon_monto=0, mientras cotizaciones.cupon_monto queda >0 (línea 128 solo actualiza si vino código) → fila inconsistente. Matiz de severidad: como calc() ya muestra al cliente el total sin cupón desde la carga, el cliente acepta el monto que ve — no hay divergencia entre lo aceptado y lo cobrado; el daño es que el descuento otorgado por el asesor se evapora silenciosamente en display, total y venta, más la inconsistencia de datos. Por eso ajusto de crítico a alto.
- **Fix:** En quote_action, si $body['cupon_codigo'] viene vacío, usar cotizaciones.cupon_codigo/cupon_monto guardados (ya cargados en $cot_data) como fallback antes de recalcular.

### 🟠 ALTO — `api/quote_action.php:238`
**CAPI Lead y QuoteRejected nunca se envían: $empresa_id no existe en quote_action.php (TypeError silenciado)**

- **Evidencia:** Línea 238: `MarketingPixels::capi_lead($empresa_id, (float)$cot['total'], $empresa['moneda'] ?? 'MXN')` y línea 317: `capi_rechazar($empresa_id)`. $empresa_id y $empresa NUNCA se asignan en el archivo (grep confirma solo esas 2 apariciones; Router::load_api en core/Router.php:281-288 no inyecta variables). Además el SELECT de línea 28 no incluye `total`. capi_lead está tipada `int $empresa_id` (MarketingPixels.php:233) → pasar null lanza TypeError, tragado por `catch (\Throwable)`.
- **Impacto:** El evento de conversión Lead a Meta (feature de Marketing del plan Business, cobrado a $799/mes) jamás se dispara al aceptar ninguna cotización de ninguna empresa; QuoteRejected tampoco. Las campañas de retargeting de los clientes Business optimizan sin datos de conversión y nadie lo nota porque el error se silencia.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código: $empresa_id y $empresa no se asignan en ningún punto de api/quote_action.php y Router::load_api (Router.php:281-288) hace require dentro de un método estático sin inyectar variables. Escenario mínimo: cualquier cliente acepta una cotización desde el slug público → línea 238 llama MarketingPixels::capi_lead(null, ...) con parámetro tipado int no-nullable (MarketingPixels.php:233, sin strict_types en el repo pero null nunca se acepta para int userland) → TypeError → tragado por catch(\Throwable) vacío → el evento Lead nunca se envía a Meta. Idéntico en línea 317 con capi_rechazar($empresa_id). Ocurre en el 100% de aceptaciones/rechazos, no es edge case. Además el SELECT de línea 28 no trae 'total', por lo que el value sería 0 aun sin el TypeError. Fix: usar EMPRESA_ID y $total_guardar/$empresa_mail['moneda'].
- **Fix:** Usar EMPRESA_ID en ambas llamadas y $total_guardar como valor: `MarketingPixels::capi_lead(EMPRESA_ID, $total_guardar, $mon)` leyendo la moneda de empresas.

### 🟠 ALTO — `api/quote_action.php:82`
**quote_action acepta cupones VENCIDOS: solo valida activo=1, no vencimiento**

- **Evidencia:** Líneas 81-84: `SELECT id, porcentaje, monto_fijo FROM cupones WHERE empresa_id=? AND codigo=? AND activo=1` — no consulta vencimiento_tipo/vencimiento_dias/vencimiento_fecha. La validación de expiración solo existe en el JS del slug (cotizacion.php:1333 `const ok = c && (!c.exp || now < new Date(c.exp));`). Además TODOS los códigos activos con su fecha se imprimen en el HTML del slug (línea 1276 `const COUPONS = ...`).
- **Impacto:** Cualquier cliente puede leer los códigos del código fuente del slug y hacer POST directo a /api/quote-action con un cupón vencido (o uno que el asesor nunca le ofreció): el servidor aplica el descuento, sobrescribe el total y crea la venta descontada. La empresa cobra menos de lo que autorizó su propia regla de vigencia.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. (1) La tabla cupones SÍ tiene reglas de vigencia (vencimiento_tipo ENUM('nunca','fecha_fija','dias_cotizacion'), vencimiento_dias, vencimiento_fecha — migrations/cotizacl_cotizacloud.sql:3127-3129) y la UI de config las exige (modules/config/cupon.php:37-44). (2) api/quote_action.php:81-84 re-valida el cupón server-side SOLO con `activo=1` — no consulta ninguna columna de vencimiento (tampoco usos_max). Si el cupón existe y está activo, aplica el descuento (líneas 87-92), sobrescribe cotizaciones.total con el total descontado (117-125), incrementa usos (133) y crea la venta con ese total (156-177). (3) La expiración solo se valida en el JS del slug: public/cotizacion.php:1333 `const ok = c && (!c.exp || now < new Date(c.exp))`. (4) Los códigos SÍ se exponen: la query del slug (cotizacion.php:118-121) trae TODOS los cupones activos de la empresa (incluidos los ya vencidos, porque el filtro es solo activo=1) y los imprime en el HTML en `const COUPONS` (línea 1276) con código, pct, monto_fijo y fecha exp. ESCENARIO MÍNIMO: empresa tiene cupón 'PROMO20' con vencimiento_fecha pasada pero activo=1 (nada desactiva activo al vencer — la vigencia se aplica solo vía JS). El cliente abre el slug, lee 'PROMO20' en el view-source, y hace POST directo a /api/quote-action con {cotizacion_id, accion:'aceptar', nombre:'X', cupon_codigo:'PROMO20'}: el servidor acepta el cupón vencido, guarda el total con 20% menos y crea la venta descontada. También funciona con cualquier cupón activo de la empresa que el asesor nunca ofreció para esa cotización, y con cupones agotados (usos_max tampoco se valida). Nota: guardar.php:80 (editor interno) tiene la misma omisión, pero ahí hay login+permiso; el vector real es este endpoint público sin autenticación. Severidad 'alto' es correcta: pérdida financiera directa, explotable sin credenciales, mitigado solo porque requiere que exista un cupón vencido/no ofrecido aún marcado activo.
- **Fix:** Replicar en el WHERE/PHP del quote_action la lógica de expiración de cotizacion.php:129-135 (fecha_fija y dias_cotizacion contra created_at de la cot) antes de aplicar el cupón.

### 🟡 MEDIO — `modules/cotizaciones/guardar.php:151`
**Editar una cotización con descuento automático VENCIDO sigue restándolo del total guardado**

- **Evidencia:** Líneas 98-103: si el descuento ya estaba activo se conserva la fecha original (`$desc_auto_expira = $expira_actual;`) aunque esté en el pasado. Línea 151: `if ($desc_auto_activo) { $desc_auto_amt = $base * ($desc_auto_pct / 100); $base -= $desc_auto_amt; }` — aplica el descuento SIN verificar expiración. En cambio el slug (cotizacion.php:96 `if ($adc_exp && $adc_exp < time()) { $adc_on = false; }`) y quote_action (líneas 101-102) sí verifican la expiración y NO lo aplican.
- **Impacto:** Asesor edita una cotización cuyo descuento de 3 días ya venció: guardar.php almacena total con descuento (ej. $9,000), pero el cliente ve $10,000 en el slug y al aceptar se cobra $10,000. Lista y reportes del asesor muestran $9,000. Tres totales inconsistentes por una simple edición post-expiración.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Escenario mínimo: cotización con descuento automático de 3 días vencido (descuento_auto_activo=1 en BD, descuento_auto_expira en el pasado); el asesor abre el editor y guarda cualquier cambio trivial. ver.php:729 inicializa descAutoActivo desde la BD sin checar expiración y ver.php:973 manda descuento_auto_activo:1 en el payload; guardar.php:99-100 conserva la fecha vencida original y guardar.php:151 resta el descuento del total SIN verificar $desc_auto_expira (no existe ningún check de expiración en el archivo). En contraste, cotizacion.php:96 y quote_action.php:100-105 sí verifican expiración. Resultado: cotizaciones.total queda con descuento (ej. $9,000) mientras el slug muestra $10,000 y al aceptar se cobra $10,000 — lista/reportes/lo que comunica el asesor difieren de lo que paga el cliente. Ajusto de alto a medio porque no hay pérdida monetaria: quote_action recalcula server-side y cobra el total correcto; el daño es inconsistencia de datos y confusión del asesor, no cobro erróneo.
- **Fix:** En guardar.php, antes de la línea 151: si $desc_auto_expira ya pasó (strtotime < time()), poner $desc_auto_activo=0 y $desc_auto_amt=0 (o renovar la fecha explícitamente si el asesor reactiva).

### 🟡 MEDIO — `public/cotizacion.php:1374`
**Descuento fijo + descuento automático: el slug muestra un total y el servidor cobra otro (orden de aplicación invertido)**

- **Evidencia:** JS del slug: línea 1366 aplica primero el descuento auto (`aa = SUB * AUTO.pct / 100; tot -= aa;`) y luego el cupón sobre el remanente (línea 1374 `ca = applied.monto_fijo ? Math.min(applied.monto_fijo, tot) : tot * applied.pct / 100`). quote_action hace lo inverso: cupón sobre subtotal completo (línea 88 `min((float)$cupon_real['monto_fijo'], $subtotal_srv)`) y descuento auto después (línea 103, sobre base_after_cupon). Con % es conmutativo, con monto_fijo NO.
- **Impacto:** SUB $10,000, descuento auto 10%, cupón fijo $500: el cliente ve y confirma $8,500 en el modal de aceptar; el servidor guarda y factura $8,550 (desc = $9,500×10% = $950). Diferencia real de $50 entre lo aceptado y lo cobrado — reclamo directo del cliente final.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo ambos archivos. JS (public/cotizacion.php calc(), líneas 1362-1378): aplica descuento auto primero (línea 1366: aa = SUB*AUTO.pct/100) y el cupón sobre el remanente (línea 1374: ca = min(monto_fijo, tot) con tot ya reducido). Ese total es el que el cliente confirma en el modal de aceptar (openM líneas 1392-1401). Servidor (api/quote_action.php): cupón fijo sobre subtotal completo (línea 88: min(monto_fijo, $subtotal_srv)) y descuento auto después sobre $base_after_cupon (líneas 98-103); guarda $total_guardar ignorando el total del cliente. Con porcentajes el orden es conmutativo, con monto fijo NO: diferencia = monto_fijo × pct_auto. Escenario mínimo que lo dispara: cotización con descuento automático activo 10% + cupón de monto fijo $500 sobre subtotal $10,000 → el modal muestra y el cliente acepta $8,500 (10000−1000−500), el servidor guarda y factura $8,550 (10000−500−950). Cobra $50 más de lo aceptado; con IVA modo suma la diferencia se amplifica por 1.16. El JS sí soporta cupones monto_fijo (línea 1348) y el descuento auto se muestra con countdown, así que la combinación es alcanzable en producción.
- **Fix:** Unificar el orden: aplicar en el JS del slug el cupón primero y el descuento auto sobre el remanente, igual que guardar.php y quote_action.

### 🟡 MEDIO — `modules/cotizaciones/clonar.php:82`
**Clonar copia el total CON descuento de cupón pero no copia el cupón: el clon nace con total inconsistente con sus propias líneas**

- **Evidencia:** El INSERT (líneas 63-96) copia `$cot['subtotal']` y `$cot['total']` tal cual pero omite cupon_id/cupon_codigo/cupon_pct/cupon_monto (no están en la lista de columnas). También copia descuento_auto_expira posiblemente vencida con descuento_auto_amt ya restado del total. El slug del clon recalcula desde las líneas (cotizacion.php:99-115) con cupon_monto=0 → muestra un total MAYOR que el guardado.
- **Impacto:** Original: $10,000 con cupón 10% → total $9,000. El clon lista $9,000 en el editor/listado, pero el cliente ve $10,000 en el slug y al aceptar la venta se crea por $10,000. El asesor cree que ofreció $9,000. Discrepancia silenciosa en cada clonación de cotizaciones con cupón.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Escenario mínimo: (1) asesor con permiso aplicar_descuentos guarda una cotización con cupón 10% sobre $10,000 → guardar.php:148-179 persiste total=$9,000 y cupon_monto=$1,000; (2) asesor clona → clonar.php:63-96 copia subtotal ($10,000) y total ($9,000) pero su lista de columnas omite cupon_id/cupon_codigo/cupon_pct/cupon_monto → el clon queda con total=$9,000 y cupon_monto=0 (default del schema); (3) el listado (lista.php:439) muestra $9,000 al asesor, pero el slug público (cotizacion.php:101-115) recalcula desde las líneas con cupon_monto_guardado=0 y muestra $10,000 al cliente (línea 906); (4) al aceptar, quote_action.php:74-124 recalcula server-side sin cupón → la venta se crea por $10,000. Discrepancia silenciosa asesor/cliente en cada clonación de cotización con cupón. El punto del descuento_auto_expira vencido también es real (cotizacion.php:96,105 recalcula a 0 para estados no aceptados). Severidad medio es honesta: requiere cupón previo + clonación; el cliente sí ve y acepta el monto mayor (no hay cobro oculto al cliente), pero el asesor opera con un total falso y el trato ofrecido se pierde en silencio.
- **Fix:** Copiar también cupon_id, cupon_codigo, cupon_pct y cupon_monto en el INSERT del clon (y anular descuento_auto_* si descuento_auto_expira ya pasó).

### 🟡 MEDIO — `modules/cotizaciones/enviar.php:32`
**Re-enviar una cotización ya VISTA regresa su estado a 'enviada' (parece que el cliente nunca la abrió)**

- **Evidencia:** Línea 23 permite estados ['borrador','enviada','vista'] y línea 32 ejecuta `UPDATE cotizaciones SET estado='enviada', enviada_at=COALESCE(enviada_at, NOW())` incondicionalmente. Si estado era 'vista', regresa a 'enviada' aunque vista_at/visitas sigan poblados. lista.php:132 y el filtro de chips (línea 344) muestran el badge según `estado`. Además enviar.php no tiene ningún check de permiso ni ownership (solo csrf_check, línea 11).
- **Impacto:** El asesor toca 'Enviar' de nuevo (para copiar el link) sobre una cotización que el cliente YA abrió: la lista vuelve a mostrarla como 'Enviada'/sin abrir, contradiciendo el historial de visitas y las métricas de apertura del Radar/dashboard. Estado y *_at quedan inconsistentes.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. (1) enviar.php:23 permite explícitamente estado 'vista' y la línea 32 ejecuta `UPDATE cotizaciones SET estado='enviada', enviada_at=COALESCE(enviada_at, NOW())` sin condición sobre el estado previo — una cotización 'vista' regresa a 'enviada'. (2) El escenario es alcanzable desde la UI: en ver.php:151 `$es_editable` incluye 'vista', y en ver.php:662 el overlay de compartir muestra el botón "Marcar como enviada" que llama enviarCotizacion() → POST /cotizaciones/:id/enviar. Escenario mínimo: asesor abre una cotización que el cliente YA vio, abre el diálogo de compartir link y pulsa "Marcar como enviada" → estado regresa a 'enviada'. (3) Efecto visible: lista.php:132 st_badge renderiza el badge por `estado` → muestra 'Enviada' (que en el producto significa "sin abrir"), y el chip 'Vista' (línea 344) deja de incluirla, contradiciendo vista_at/visitas que siguen poblados. (4) Efecto secundario ADICIONAL no citado en el hallazgo: public/cotizacion.php:302 hace `SET estado='vista', vista_at=NOW() WHERE estado='enviada'` — como el estado volvió a 'enviada', la SIGUIENTE visita del cliente SOBREESCRIBE vista_at con NOW(), destruyendo el timestamp de primera apertura. (5) El propio equipo considera vista→enviada una regresión indebida: en sesión 19 mayo se quitó a propósito el revert vista→enviada del ghost cleanup ("irreversible ahora"), y en sesión 10 mayo se protegió enviada_at con COALESCE precisamente en este archivo — pero se dejó el estado sin proteger. MATIZ que reduce el impacto declarado: las métricas "sin abrir" del dashboard (dashboard/index.php:262-264 y 333-334) exigen `vista_at IS NULL`, y ActividadScore:208 cuenta vistas con `OR visitas > 0` — por lo tanto dashboard y termómetro NO se corrompen (salvo el caso de sobreescritura de vista_at descrito en el punto 4). El daño principal es el badge/filtro de la lista y la pérdida del vista_at original en la siguiente visita. También es cierto que el endpoint solo valida empresa_id + CSRF, sin check de puede_editar_cotizaciones (la UI oculta el botón pero el endpoint no lo exige).
- **Fix:** Cambiar a `SET estado = IF(estado='vista','vista','enviada'), enviada_at=COALESCE(enviada_at,NOW())` (o solo tocar estado cuando venga de 'borrador') y agregar check de permiso/ownership.

### 🟡 MEDIO — `modules/cotizaciones/crear.php:26`
**Límite de 25 del plan Free con race condition y crear.php acepta cotizaciones sin líneas**

- **Evidencia:** Líneas 26-29: `$trial = trial_info($empresa_id); if ($trial['agotado']) json_error(...)` — el conteo se hace ANTES y FUERA de la transacción que abre en línea 163, sin lock. Dos requests simultáneos con 24 usadas pasan ambos. Además, a diferencia de guardar.php:143-146 (`if (empty($lineas)) json_error(...)` + exige un precio > 0), crear.php no valida items: líneas 97-131 permiten $items vacío y el INSERT crea una cotización estado 'enviada' con total $0.
- **Impacto:** Empresas Free pueden superar el límite de 25 (el gancho de venta del upgrade) con doble clic o requests paralelos; y por API se crean cotizaciones 'enviadas' vacías con total 0 que ensucian listas, embudo de conversión y métricas del termómetro.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código. (1) Race: crear.php:26-29 evalúa trial_info() ANTES de DB::beginTransaction() (línea 163); trial_info() usa SELECT COUNT(*) plano sin FOR UPDATE (Helpers.php:428) y 'agotado' = usadas >= TRIAL_LIMIT (línea 457). Escenario mínimo: empresa Free con 24 cotizaciones + doble clic (dos POST concurrentes a /cotizaciones/nueva) → ambos leen COUNT=24, ambos pasan, quedan 26. No hay re-chequeo dentro de la transacción ni constraint en BD. (2) Items vacíos: crear.php:97-98 acepta items=[] sin error; el foreach no itera, subtotal=0, total=0, y el INSERT (línea 179) crea estado='enviada' + enviada_at=NOW() (línea 209). Escenario mínimo: POST JSON {"titulo":"x","cliente_id":N,"items":[]} crea cotización enviada con total $0. El contraste citado es exacto: guardar.php:143-146 sí valida empty($lineas) y precio>0; crear.php no tiene ninguna de las dos validaciones.
- **Fix:** Mover el check de límite dentro de la transacción con `SELECT COUNT(*) ... FOR UPDATE` (o UNIQUE folio-based); y replicar en crear.php las validaciones de líneas de guardar.php:143-146.

### ⚪ BAJO — `modules/cotizaciones/ver.php:944`
**El editor muestra $0 de descuento para cupones de monto fijo (solo calcula por porcentaje)**

- **Evidencia:** calcularTotales() línea 944: `if(cuponSeleccionado){cuponAmt=subRegular*(cuponSeleccionado.pct/100);base-=cuponAmt;}` — nunca considera monto_fijo. guardar.php:150 sí lo aplica: `$cupon_monto = $cupon_monto_fijo !== null ? min($cupon_monto_fijo, $subtotal) : ...`. Un cupón de monto fijo tiene porcentaje 0 → el editor muestra descuento $0 y total completo.
- **Impacto:** El asesor aplica un cupón de $500 fijos: el editor le muestra el total SIN descuento, pero al guardar el total almacenado sí lo resta. El asesor no puede confiar en lo que ve mientras edita — comunica un precio al cliente y el sistema guarda otro $500 menor.
- **Fix:** En calcularTotales(): `cuponAmt = cuponSeleccionado.monto_fijo ? Math.min(cuponSeleccionado.monto_fijo, subRegular) : subRegular*(cuponSeleccionado.pct/100);` (exponer monto_fijo en el objeto del cupón).

### 🟡 MEDIO — `api/quote_action.php:117`
**Al aceptar con cupón/descuento, impuesto_amt queda obsoleto en la fila (solo se actualiza total)**

- **Evidencia:** Líneas 117-125: `UPDATE cotizaciones SET estado='aceptada', aceptada_at=NOW(), accion_at=NOW(), total=?` — el total se recalcula con cupón del cliente y descuento vigente, pero impuesto_amt (y cupon_pct si el cliente aplicó cupón nuevo distinto al guardado) no se recalculan; conservan los valores del último guardar.php, calculados sin ese cupón.
- **Impacto:** Una cotización aceptada con cupón aplicado por el cliente queda con impuesto_amt inconsistente con su propio total (ej. total $8,550 con IVA registrado de $1,600 correspondiente a la base sin cupón). Cualquier reporte fiscal o desglose que use impuesto_amt de cotizaciones aceptadas muestra cifras que no cuadran con el total.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código: el UPDATE de aceptación (api/quote_action.php:117-125) persiste total recalculado con cupón/descuento pero no impuesto_amt, y guardar.php:148-158 demuestra que impuesto_amt se calcula sobre la base post-descuentos — por lo que al aceptar con cupón nuevo la fila queda con IVA de una base mayor, inconsistente con su propio total. El fix declarado es realista (variables $base_srv/$imp_pct/$imp_modo ya en scope). Sin embargo la evidencia exagera: cupon_pct SÍ se actualiza (línea 131-132), el slug público recalcula el IVA en vivo (public/cotizacion.php:108-113, no afectado), y NINGÚN reporte consume impuesto_amt (verificado con grep). El impacto real verificado es el estado de cuenta imprimible de ventas (modules/ventas/ver.php:999, documento cliente-facing cuyo desglose no cuadra) y el panel del editor (cotizaciones/ver.php:543). Dinero real (total, saldo, pagos) correcto. Bug de consistencia real con superficie acotada → severidad media, no alta.
- **Fix:** En el mismo UPDATE de aceptación, persistir también impuesto_amt recalculado: `round($base_srv * $imp_pct/100, 2)` para modo 'suma' y la fórmula de 'incluido' para ese modo.


---

## Ventas / Dinero

### 🔴 CRÍTICO — `api/quote_action.php:73`
**El total que acepta el cliente en el slug NO es el total con que se crea la venta cuando hay extras**

- **Evidencia:** quote_action.php:73-75 suma TODAS las líneas sin filtrar extras: `SELECT COALESCE(SUM(subtotal),0) FROM cotizacion_lineas WHERE cotizacion_id=?` y luego les aplica cupón, descuento auto e IVA (líneas 98-114). En cambio public/cotizacion.php:99 calcula `$subtotal = array_sum(array_column($lineas,'subtotal'))` SOLO con principales (línea 80 filtra `empty($l['es_extra'])`), suma extras después de impuestos/descuentos (línea 115: `$total_base = $base + ... + $subtotal_extras`), y el JS es peor: `const SUB = <?= (float)$subtotal ?>` (1273, sin extras), `calc()` corre al cargar (línea 1817) y sobreescribe #tTot con un total que EXCLUYE los extras por completo — también en el modal de aceptar (openM, 1392-1404).
- **Impacto:** Cotización con principal $10,000 + extra $2,000 e IVA suma 16%: el cliente ve Total $11,600 (JS excluye el extra) o $13,600 (render PHP, extra sin IVA), acepta, y la venta se crea con total_guardar = 12,000×1.16 = $13,920. El cliente aceptó un precio y se le cobra otro — es el peor golpe posible a la confianza del producto (feature Extras es diferenciador Business).
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Escenario mínimo: cotización con 1 línea principal + 1 línea es_extra=1, cliente abre el slug y acepta. (a) quote_action.php:73-75 suma TODAS las líneas sin filtrar es_extra y les aplica cupón (88-91), descuento auto (98-104) e IVA (110-111) → total_guardar incluye extras con impuestos/descuentos, y se escribe en cotizaciones.total y en la venta (total y saldo, líneas 172-173). (b) El slug renderiza otro número: cotizacion.php:80 filtra extras, :99 subtotal solo principales, :115 suma extras después de IVA/descuentos (extras sin IVA). (c) El JS es aún peor: SUB=<?=$subtotal?> (1273, sin extras), calc() corre al cargar (1817) y sobreescribe #tTot con un total que EXCLUYE los extras; el modal de aceptar (openM 1390-1406) muestra ese mismo total al cliente. (d) guardar.php:128,148-158 (editor) usa la fórmula de quote_action (todas las líneas + cupón + IVA), confirmando que el slug es el divergente — hay 3 fórmulas distintas conviviendo. Ejemplo numérico verificado: principal $10,000 + extra $2,000, IVA suma 16% → cliente confirma $11,600 en el modal, la venta se crea con $13,920. Ni siquiera requiere IVA: sin impuestos ni descuentos, el modal muestra $10,000 y la venta $12,000. Severidad crítica: el cliente acepta un precio y el sistema registra/cobra otro, en el flujo core de aceptación, con Extras como feature Business activo.
- **Fix:** Unificar la fórmula: decidir si los extras entran o no a descuentos/impuestos y aplicarla igual en quote_action.php (filtrar es_extra o no), en el render PHP del slug y en el JS (pasar SUB_EXTRAS al calc() y al modal de aceptar).

### 🟠 ALTO — `public/recibo.php:48`
**Recibo público muestra 'Total pagado' incorrecto: mezcla el total VIVO de la venta con el saldo snapshot del recibo**

- **Evidencia:** `$pagado_despues = $venta_total - $saldo_despues;` donde `$venta_total = (float)$rec['venta_total']` viene de `v.total` actual (línea 16, JOIN vivo) y `saldo_despues` es el snapshot congelado al momento del abono. Se muestra en línea 240 como monto pagado a la fecha del recibo.
- **Impacto:** Recibo: pagado_antes $0, monto $500, saldo_despues $500 (total era $1,000). Después se agrega un extra y el total sube a $2,000. El cliente reabre su recibo (link WhatsApp) y ve 'pagado' $1,500 cuando solo ha pagado $500 — o al revés, tras cancelar líneas ve menos de lo que pagó. Documento de dinero público con cifra falsa.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código real. recibo.php:48 calcula pagado_despues = v.total VIVO (JOIN línea 16/22) menos recibos.saldo_despues, que es snapshot congelado al momento del abono (abono.php:57,83) y jamás se actualiza (el único UPDATE recibos es el flag cancelado en cancelar_recibo.php:42). El total de la venta SÍ cambia después de emitir recibos: ventas/guardar.php:109, agregar_extra.php:89, eliminar_extra.php:80 y acciones.php:124/186/192/234/254 reescriben ventas.total sin tocar recibos. Escenario mínimo: venta $1,000 → abono $500 (recibo con pagado_antes=0, saldo_despues=500) → se agrega extra de $1,000 (total=2,000) → cliente reabre /r/:token y la línea 240 muestra 'Pagado total' = 2000−500 = $1,500 habiendo pagado $500. La misma tarjeta muestra $0 + $500 = $1,500: aritmética autocontradictoria en un comprobante público de dinero. Caso inverso al eliminar líneas: muestra menos de lo pagado. Es bug de display (la BD está correcta: pagado_antes + monto están bien guardados), por eso alto y no crítico; el fix trivial es pagado_despues = pagado_antes + monto (o −monto si es cancelación).
- **Fix:** Calcular con puros snapshots: `$pagado_despues = $pagado_antes + ($es_cancelacion ? 0 : $monto);` y no derivar nada del total vivo.

### 🟠 ALTO — `modules/ventas/abono.php:51`
**Folios de recibo generados con MAX(id) global: saltan según la actividad de OTRAS empresas y pueden duplicarse en concurrencia**

- **Evidencia:** `$max_rec = (int)DB::val("SELECT COALESCE(MAX(id),0) FROM recibos WHERE empresa_id=?", ...); $numero_rec = 'REC-' . date('Y') . '-' . str_pad($max_rec + 1, 4, ...)`. `id` es el auto_increment GLOBAL de la tabla (compartido entre todas las empresas). El dump real muestra empresa 2 con recibos REC-2026-0001..0011 e ids 22..32: su próximo folio con este código sería REC-2026-0033 (salta 0012-0032). Además el SELECT no bloquea: dos abonos simultáneos en ventas distintas de la misma empresa leen el mismo MAX(id) y generan el MISMO numero (schema recibos sin UNIQUE en numero, migrations/cotizacl_cotizacloud.sql:3783-3802). ver.php:59-60 encima muestra como 'siguiente folio' COUNT(*)+1, que no coincide con el que realmente se genera.
- **Impacto:** Cliente con 11 recibos recibe de pronto el REC-2026-0033: parece que le ocultan pagos, y filtra volumen de otras empresas. Dos cajeros simultáneos emiten dos recibos con el mismo folio — imposible de auditar.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. (1) modules/ventas/abono.php:51-52 genera el folio con `SELECT COALESCE(MAX(id),0) FROM recibos WHERE empresa_id=?` + str_pad(max+1): `id` es el AUTO_INCREMENT de la tabla, compartido entre TODAS las empresas, así que el número de la cláusula WHERE devuelve el id global más alto de esa empresa, no su conteo. Con el dump real (empresa 2: recibos REC-2026-0001..0011 con ids 22..32, AUTO_INCREMENT=33 en migrations/cotizacl_cotizacloud.sql:3809-3819,4638), el próximo abono de empresa 2 generaría REC-2026-0033 — salta 0012-0032 según la actividad de otras empresas y filtra volumen ajeno. (2) Escenario mínimo de duplicado: dos cajeros de la misma empresa registran abonos SIMULTÁNEOS en ventas DISTINTAS. El `FOR UPDATE` de la línea 41 solo bloquea la fila de `ventas` (distinta para cada uno); el SELECT MAX(id) de la línea 51 es lectura no bloqueante, ambos leen MAX(id)=32 y ambos insertan `numero`='REC-2026-0033'. El schema de recibos NO tiene UNIQUE en `numero` (solo PK id, UNIQUE token, líneas 4433-4437) → ambos INSERT tienen éxito con el mismo folio. El comentario "// Folio atómico" del código es falso. (3) modules/ventas/ver.php:59-60 muestra como 'siguiente folio' COUNT(*)+1 (REC-2026-0012 para empresa 2), que no coincide con el que abono.php realmente genera (0033). Todo verificado contra código y dump; severidad 'alto' es honesta: es corrupción de folios en documentos financieros (recibos de pago) — saltos visibles al cliente, duplicados imposibles de auditar y fuga de volumen entre tenants — pero no es explotable remotamente ni corrompe montos/saldos.
- **Fix:** Usar el mecanismo atómico que ya existe: `DB::siguiente_folio($empresa_id, 'REC', 'REC')` (core/DB.php:111) como se hace con VTA, y agregar UNIQUE (empresa_id, numero) en recibos.

### 🟠 ALTO — `modules/ventas/acciones.php:174`
**Descuento manual y descuento automático se borran mutuamente al recalcular el total de la venta**

- **Evidencia:** Acción 'descuento': `$nuevo_subtotal = $subtotal_lineas - (float)($cot['cupon_monto']??0) - $desc_amt;` — el SELECT de línea 171 ni siquiera trae descuento_auto_amt, así que el recálculo lo omite. A la inversa, ventas/guardar.php:92 hace `$base = $subtotal_lineas - $cupon_amt - $desc_auto_amt;` sin restar descuento_manual_amt. Además en 'descuento' el saldo no se acota: `$nuevo_saldo = round($nuevo_total - (float)$venta['pagado'], 2)` (181) puede quedar negativo, y la rama IVA suma (176) no clampa base negativa.
- **Impacto:** Venta con descuento automático de $500 copiado de la cotización: el admin aplica un descuento manual de $100 y el total SUBE $400 (el auto desaparece del cálculo). Luego alguien guarda desde el editor y ahora desaparece el manual. El cliente ve el total cambiar sin razón — desconfianza directa en los números.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Escenario mínimo: (1) Cliente acepta cotización con descuento automático activo → api/quote_action.php:107-113 crea la venta con total = subtotal − cupón − desc_auto y guarda ventas.descuento_auto_amt (línea 160/174). (2) El admin aplica descuento manual vía POST /ventas/:id/descuento → modules/ventas/acciones.php:171 hace SELECT de solo cupon_monto/impuesto_* (NO trae descuento_auto_amt) y la línea 174 recalcula `$nuevo_subtotal = $subtotal_lineas - cupon_monto - $desc_amt` — el descuento automático desaparece del total: con auto de $500 y manual de $100, el total SUBE $400. (3) A la inversa, cualquier guardado del editor (modules/ventas/guardar.php:92) hace `$base = $subtotal_lineas - $cupon_amt - $desc_auto_amt` sin restar descuento_manual_amt (la variable no se lee en ningún punto del archivo), borrando el manual del total aunque la columna ventas.descuento_manual_amt conserve su valor (ver.php:85 lo sigue mostrando → UI inconsistente con el total). También confirmados los detalles secundarios: acciones.php:181 no acota el saldo (`round($nuevo_total - pagado)` puede quedar negativo, a diferencia de guardar.php:98 que sí hace max(0,...)), y la rama impuesto_modo='suma' (acciones.php:176) multiplica una base sin clamp que puede ser negativa. Severidad alto es correcta: corrompe totales financieros visibles al cliente con dos operaciones normales de admin, pero requiere coexistencia de ambos tipos de descuento y es recuperable reeditando; no es escalación de seguridad (crítico).
- **Fix:** Ambos recálculos deben restar los DOS descuentos: en acciones.php incluir descuento_auto_amt en el SELECT y en la resta; en guardar.php restar también descuento_manual_amt; acotar saldo con max(0) y base con max(0) antes del IVA.

### 🟠 ALTO — `modules/ventas/acciones.php:124`
**agregar-item y editar-linea suman/restan el subtotal directo al total sin aplicar IVA ni descuentos (y sin round)**

- **Evidencia:** 'agregar-item': `UPDATE ventas SET total=total+?, saldo=saldo+?` con `$subtotal = $cantidad * $precio;` (102, sin round). 'editar-linea': `UPDATE ventas SET total=GREATEST(0,total-?), saldo=GREATEST(0,saldo-?)` (234) y `total=GREATEST(0,total+diff)` (254). Ninguno considera impuesto_modo='suma' ni cupón/descuentos, a diferencia de guardar.php:92-97 que sí recalcula todo con IVA.
- **Impacto:** Empresa con IVA suma 16%: admin agrega item de $1,000 → total sube $1,000 en vez de $1,160. En cuanto alguien usa 'Guardar cambios' (recálculo completo), el total brinca $160 'solo'. Con cantidad 0.333×$29.99 sin round el total acumula residuo flotante y el saldo nunca cierra exacto en $0. GREATEST(0,...) por separado puede desincronizar total y saldo permanentemente.
- **Escenario que lo dispara (verificado):** Confirmado leyendo acciones.php completo y guardar.php. El invariante del sistema es que ventas.total incluye IVA cuando impuesto_modo='suma' (lo prueban guardar.php:92-97 y la acción 'descuento' en el MISMO acciones.php:174-181, ambos multiplican por 1+pct/100 y hacen round). Pero 'agregar-item' (línea 102: $subtotal=$cantidad*$precio sin round; línea 124: total=total+subtotal crudo) y 'editar-linea' (líneas 234 y 254: GREATEST(0,total±subtotal/diff)) suman/restan el subtotal SIN IVA, sin cupón y sin round. Escenario mínimo: empresa con IVA suma 16%, venta de $11,600; admin agrega item de $1,000 vía agregar-item → total queda $12,600 en vez de $12,760 y saldo $160 abajo; el siguiente 'Guardar cambios' o 'descuento' recalcula completo y el total brinca +$160 solo. Además GREATEST por separado desincroniza total/saldo cuando saldo < subtotal eliminado (saldo se clampa a 0, total no), rompiendo saldo=total-pagado. Matiz: se auto-corrige al próximo recálculo completo y solo afecta ventas con IVA suma o cupón/descuento, pero mientras tanto el saldo erróneo gobierna abonos y estado pagada/parcial.
- **Fix:** Reemplazar los deltas por el mismo recálculo completo de guardar.php (subtotal de líneas − cupón − descuentos, luego IVA, round 2) tras cada INSERT/UPDATE/DELETE de línea.

### 🟡 MEDIO — `api/quote_action.php:39`
**Re-aceptación de cotización ya aceptada sin autenticación: sobreescribe totales, infla usos de cupón y duplica notificaciones**

- **Evidencia:** `$estados_activos = ['enviada','vista','aceptada'];` — el check del lock (línea 62) usa el mismo array, así que una cotización YA aceptada pasa de nuevo. El endpoint es público (sin login/CSRF). En cada re-POST: se re-ejecuta `UPDATE cotizaciones SET ... total = ?` (117-125), `UPDATE cupones SET usos=usos+1` (133), `UPDATE cotizaciones SET descuento_auto_amt=?` (136) y push+email de 'Cotización aceptada' (198-230). La venta no se duplica (144) pero conserva su total original.
- **Impacto:** Cualquiera con acceso al slug puede re-postear 'aceptar' con otro cupón (o sin cupón): cotizaciones.total y descuento_auto_amt cambian mientras ventas.total queda con el valor viejo → cotización y venta reportan montos distintos, usos del cupón se inflan (métrica de marketing falsa) y el vendedor recibe pushes de aceptación repetidos.
- **Escenario que lo dispara (verificado):** CONFIRMADO. Línea 39 `$estados_activos = ['enviada','vista','aceptada']` incluye 'aceptada', y tanto el check inicial (43) como el lock FOR UPDATE (62) usan ese mismo array, así que una cotización YA aceptada re-entra al flujo completo. El endpoint es público: la ruta /api/quote-action está en el bloque sin login del Router (line 86) y load_api solo hace `require` del archivo — sin Auth ni CSRF. EMPRESA_ID viene del host. Escenario mínimo que lo dispara: cualquiera que conozca el cotizacion_id (visible en el slug público) hace un segundo POST {cotizacion_id, accion:'aceptar', nombre:'x'} a /api/quote-action. Efectos verificados en cada re-POST: se re-ejecuta UPDATE cotizaciones SET total=? (117-125), UPDATE cotizaciones SET descuento_auto_amt=? (136), y se re-disparan push (198-210) + email (213-230) de 'Cotización aceptada'. Si además se pasa un cupon_codigo válido y activo (50, re-validado server-side en 81-93), se ejecuta UPDATE cupones SET usos=usos+1 (133) inflando el uso, y cotizaciones.total cambia mientras ventas.total conserva el valor original (la venta NO se recrea por el guard de 144) → cotización y venta reportan montos distintos. Ajusto de 'alto' a 'medio': el total SÍ se recalcula server-side (no hay robo monetario directo, no hay procesamiento de pago aquí y la venta pagable no cambia); los daños reales son notificaciones duplicadas (spam al vendedor), inflación de la métrica de usos de cupón, y un desajuste de datos cotización/venta que además requiere conocer un código de cupón activo para materializarse. Es un problema de integridad de datos y spam, no de pérdida financiera.
- **Fix:** En la rama aceptar, validar contra `['enviada','vista']` (dejar 'aceptada' solo para rechazar si es intencional) o retornar idempotente 'ya aceptada' sin re-ejecutar UPDATEs ni notificaciones.

### 🟡 MEDIO — `modules/ventas/ver.php:15`
**Detalle de venta y registro de abonos sin verificación de ownership: asesor ve y abona ventas ajenas**

- **Evidencia:** ver.php carga la venta solo con `WHERE v.id = ? AND v.empresa_id = ?` — no existe ningún check de `ver_todas_ventas`/usuario_id/vendedor_id en todo el archivo, mientras lista.php:19 sí filtra (`$solo_propias = !Auth::puede('ver_todas_ventas')`). abono.php:14 solo pide `capturar_pagos` sin ownership, y en acciones.php únicamente la acción 'notas' lo valida (línea 142). Contrasta con cotizaciones/guardar.php:34 que sí verifica acceso.
- **Impacto:** Un asesor sin permiso 'ver_todas_ventas' entra a /ventas/123 (IDs secuenciales) y ve montos, cliente, teléfono y abonos de ventas de otros vendedores; con 'capturar_pagos' puede registrar abonos en ventas ajenas alterando su saldo/estado. En empresas Business multi-vendedor rompe la promesa del control de permisos por usuario.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código real. (1) VER: `modules/ventas/ver.php:15-31` carga la venta solo con `WHERE v.id = ? AND v.empresa_id = ?`; en TODO el archivo no existe ningún check de ownership ni de `ver_todas_ventas` (grep completo del archivo: solo aparecen `capturar_pagos`, `cancelar_recibos`, `aplicar_descuentos`, `ver_cantidades`, `eliminar_items_venta` — todos para flags de UI, ninguno bloquea la carga). El Router (línea 185) despacha `GET /ventas/:id` directo sin middleware de ownership. En contraste, `lista.php:19,24,36` sí filtra con `$solo_propias = !Auth::puede('ver_todas_ventas')` → `(v.usuario_id=? OR v.vendedor_id=?)`, y `Auth::puede()` (Auth.php:396-426) devuelve el flag granular real para no-admins. (2) ABONO: `abono.php:14` solo valida `Auth::es_admin() || Auth::puede('capturar_pagos')` y luego carga la venta con `WHERE id=? AND empresa_id=?` (línea 40-43) — sin ownership; el INSERT del recibo y el UPDATE de pagado/saldo/estado proceden. (3) `acciones.php` solo valida ownership en la acción 'notas' (línea 142); 'estado'/'cancelar'/'agregar-item' exigen admin, pero eso confirma que el patrón de ownership existe y ver.php/abono.php no lo aplican. ESCENARIO MÍNIMO: en una empresa Business con 2 vendedores, el asesor A (rol no-admin, `puede_ver_todas_ventas=0`, `puede_capturar_pagos=1`) navega a `/ventas/123` donde 123 es una venta del asesor B (IDs secuenciales, la suya es 122): ve cliente, teléfono, email, montos y abonos completos; luego hace POST `/ventas/123/abono` con `{monto: 500}` y el sistema registra el recibo, altera pagado/saldo y puede cambiar el estado a 'parcial' o 'pagada' de una venta ajena. Severidad: se mantiene en medio — requiere usuario autenticado de la misma empresa (no cruza tenant) y solo aplica a empresas multi-vendedor, pero rompe la promesa de permisos por usuario del plan Business y permite alterar saldos de ventas ajenas.
- **Fix:** En ver.php, abono.php y las acciones de acciones.php replicar el gate de 'notas': si no tiene ver_todas_ventas y no es usuario_id/vendedor_id de la venta → 403.

### 🟡 MEDIO — `modules/ventas/guardar.php:20`
**Recálculo de saldo con 'pagado' leído sin lock: guardar.php y cancelar_recibo.php pueden pisar un abono concurrente**

- **Evidencia:** guardar.php lee la venta ANTES de la transacción y sin lock: `$venta = DB::row("SELECT * FROM ventas WHERE id=? AND empresa_id=?"...)` (20) y luego dentro de la transacción usa ese valor viejo: `$nuevo_saldo = max(0, round($nuevo_total - (float)$venta['pagado'], 2))` (98). cancelar_recibo.php hace lo mismo (SELECT con JOIN en línea 25-31, UPDATE con esos valores en 64-67). abono.php sí usa `FOR UPDATE` (41), pero eso no protege a estos dos.
- **Impacto:** Cajero registra abono de $5,000 (commit) mientras el admin guarda una edición de líneas que leyó pagado=$0: el UPDATE de guardar.php escribe saldo=total ignorando el pago recién registrado. El recibo existe pero el saldo de la venta no lo refleja — dinero 'desaparecido' hasta un recálculo manual.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. guardar.php:20 lee la venta SIN lock y FUERA de la transacción (beginTransaction en línea 48); en línea 98 calcula $nuevo_saldo = $nuevo_total - $venta['pagado'] con ese valor viejo y en línea 126 lo escribe con UPDATE ventas SET total, saldo — nunca re-lee pagado dentro de la transacción. Escenario mínimo: (1) admin abre edición de venta con pagado=0 y su POST a /ventas/:id/guardar ejecuta la línea 20; (2) antes de que llegue al UPDATE de línea 126, un cajero registra abono de $5,000 vía abono.php (que sí usa FOR UPDATE, línea 41) y hace commit → pagado=5000, saldo=total-5000; (3) guardar.php escribe saldo=nuevo_total (calculado con pagado=0) → la venta queda con pagado=5000 pero saldo que ignora el pago (pagado+saldo≠total). cancelar_recibo.php es aún peor: líneas 25-31 leen v.pagado/v.total sin lock antes de la transacción y líneas 64-67 SOBRESCRIBEN pagado con el valor viejo menos el monto — un abono concurrente desaparece por completo de la columna pagado (el recibo existe pero la venta no lo refleja). El FOR UPDATE de abono.php no protege porque estos dos endpoints leen antes de adquirir cualquier lock (lost update clásico). Severidad medio es correcta: corrupción financiera real pero requiere dos usuarios concurrentes sobre la misma venta en una ventana de ms-a-segundos, y el recibo persiste como evidencia para recálculo.
- **Fix:** Mover el SELECT de la venta dentro de la transacción con `FOR UPDATE` en guardar.php y cancelar_recibo.php (mismo patrón que abono.php:40-43).

### 🟡 MEDIO — `public/venta.php:460`
**Vista pública de venta nunca muestra la línea de IVA: el desglose visible no suma al total**

- **Evidencia:** El resumen imprime Subtotal, Cupón, Descuento y Total: `<div class="tot-row"><span class="tot-lbl">Subtotal</span>...` (460) ... `<div class="tot-row final"><span class="tot-lbl">Total</span><span class="tot-val"><?= fmt_v($total) ?>` (467) — no existe ninguna fila de impuesto pese a que el query selecciona `c.impuesto_pct, c.impuesto_modo, c.impuesto_amt` (24). La versión imprimible (fac-totales, 644-652) tiene la misma omisión.
- **Impacto:** Empresa con IVA suma 16%: el cliente abre su venta pública y ve Subtotal $10,000 → Total $11,600 sin ninguna línea que explique los $1,600. Parece un cobro inflado y genera reclamos, justo en el documento que respalda los pagos.
- **Escenario que lo dispara (verificado):** CONFIRMADO. venta.php selecciona c.impuesto_pct/impuesto_modo/impuesto_amt (línea 24) pero jamás los imprime — es la única aparición de 'impuesto' en el archivo. El resumen web (460-471) y el imprimible (644-652) solo muestran Subtotal, Cupón, Descuento, Total. El total SÍ incluye impuesto cuando impuesto_modo='suma': api/quote_action.php:111 calcula total_guardar = base × (1+pct/100) y lo inserta en ventas.total (línea 172). Escenario mínimo: empresa con impuesto_modo='suma' 16%, cotización subtotal $10,000 aceptada → venta con total $11,600; el cliente abre /v/:slug y ve Subtotal $10,000 → Total $11,600 sin fila que explique los $1,600 (pantalla e impresión). La vista de cotización sí muestra la fila de IVA (cotizacion.php:903-904), confirmando que la omisión en venta.php es inconsistencia real. No se manifiesta con modo 'incluido' ni 'ninguno' (ahí total=base, sin descuadre).
- **Fix:** Agregar la fila de impuesto (label y monto, como ya lo hace el slug de cotización en cotizacion.php:904-906) en el resumen y en la vista imprimible cuando impuesto_modo != 'ninguno'.

### 🟡 MEDIO — `modules/ventas/guardar.php:73`
**Estado de cuenta imprimible usa impuesto_amt viejo: ventas/guardar.php nunca recalcula impuesto_amt ni total de la cotización**

- **Evidencia:** El UPDATE de la cotización solo escribe `subtotal, descuento_auto_amt, descuento_auto_pct, descuento_auto_activo` (73-83) — no toca `impuesto_amt` ni `total`. El estado de cuenta (ver.php:998-999) imprime ese campo congelado: `<?= e($impuesto_nombre) ?>... + <?= format_money($impuesto_amt, ...) ?>` junto al 'Total contratado' nuevo (`$total` de ventas, 1034). agregar_extra.php/eliminar_extra.php sí actualizan cotizaciones.total (77-78) pero tampoco impuesto_amt.
- **Impacto:** Venta con IVA suma: se editan líneas y el subtotal pasa de $10,000 a $20,000. El estado de cuenta impreso muestra IVA $1,600 (viejo) con Total contratado $23,200: la suma de conceptos + ajustes no da el total. El cliente que verifica la aritmética del documento encuentra el error. Además cotizaciones.total queda desfasado para reportes.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. guardar.php:73-83 actualiza cotizaciones solo con subtotal y campos de descuento_auto — nunca impuesto_amt ni total — mientras que ventas.total SÍ se recalcula con impuesto nuevo (líneas 92-97). ver.php:24 trae c.impuesto_amt viejo por JOIN, lo asigna en :79 y lo imprime en el estado de cuenta en :999 junto al Total contratado fresco (:1034, ventas.total). Escenario mínimo: venta con impuesto_modo='suma' IVA 16%, cotización original subtotal $10,000 (impuesto_amt=$1,600); se editan líneas de la venta a $20,000 vía POST /ventas/:id/guardar → ventas.total=$23,200 pero cotizaciones.impuesto_amt sigue $1,600 → el estado de cuenta impreso muestra Conceptos $20,000 + IVA $1,600 = $21,600 ≠ Total contratado $23,200. Además impuesto_amt=0 → según el original, si el subtotal sube el IVA impreso queda subestimado siempre. acciones.php (descuento-manual, editar-linea) tiene el mismo patrón: recalcula ventas.total pero no cotizaciones.impuesto_amt/total. No afecta cobros reales (pagos y saldo usan ventas.total correcto) — es inconsistencia aritmética en documento al cliente + cotizaciones.total desfasado para reportes.
- **Fix:** En ventas/guardar.php incluir `impuesto_amt=?` y `total=?` en el UPDATE de cotizaciones con los valores recalculados; mismo ajuste en agregar_extra/eliminar_extra.

### 🟡 MEDIO — `modules/ventas/guardar.php:93`
**Total de venta puede quedar NEGATIVO con IVA suma: la rama 'suma' no clampa la base y el descuento no se topa contra el subtotal**

- **Evidencia:** `$base = $subtotal_lineas - $cupon_amt - $desc_auto_amt; if ($imp_modo === 'suma') { $nuevo_total = round($base * (1 + $imp_pct / 100), 2); } else { $nuevo_total = round(max(0, $base), 2); }` — el max(0) solo existe en la rama else. `$desc_auto_amt` viene del body sin tope contra el subtotal (25). El mismo patrón sin clamp está en agregar_extra.php:70-71, eliminar_extra.php:61-62 y acciones.php:175-176. Contrasta con cotizaciones/guardar.php:152 que sí hace `$base = max(0, $base)` ANTES del impuesto.
- **Impacto:** Admin captura descuento $10,000 en venta con subtotal $5,000 e IVA suma: total = -5,000×1.16 = -$5,800 almacenado; el dashboard, reportes y la vista pública muestran una venta con total negativo, y el % pagado/progreso se rompe.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. En modules/ventas/guardar.php:92-97 la rama 'suma' aplica el impuesto directo sobre `$base = $subtotal_lineas - $cupon_amt - $desc_auto_amt` sin clamp: `$nuevo_total = round($base * (1 + $imp_pct/100), 2)` — el `max(0, $base)` solo existe en la rama else (línea 96). El descuento del body solo se clampa a >=0 (línea 25: `round(max(0, (float)$body['descuento_auto_amt']), 2)`), nunca contra el subtotal. Escenario mínimo verificado end-to-end: empresa con impuesto_modo='suma' (IVA 16%), venta con subtotal $5,000, admin (o usuario con permiso aplicar_descuentos) abre la venta y agrega descuento de $10,000 — la UI lo permite: ventas/ver.php:1647-1649 `doDescuento()` solo valida monto>0 y ACUMULA sin tope; el POST /ventas/:id/guardar calcula base=-5,000 → total=-$5,800 guardado en ventas.total. Además, si pagado>0, `nuevo_saldo = max(0,...)=0` dispara `$nuevo_estado='pagada'` (líneas 98-103): una venta con total negativo queda marcada como pagada. Segundo disparador accidental sin descuento gigante: eliminar líneas/extras hasta que el subtotal caiga debajo de cupon_monto+descuento existentes — mismo patrón sin clamp confirmado en agregar_extra.php:69-74, eliminar_extra.php:60-66 y acciones.php:175-179 (todos leen cupon/descuento de BD y solo la rama else tiene max(0)). El contraste citado es exacto: cotizaciones/guardar.php hace `$base = max(0, $base); // Nunca permitir total negativo` ANTES de aplicar impuesto suma. Severidad se mantiene en medio: requiere acción de usuario con permiso de descuentos (no es explotable por externos), pero corrompe silenciosamente datos financieros (total/saldo/estado) visibles en dashboard, reportes y slug público, y el caso del cupón puede dispararse por accidente al editar líneas.
- **Fix:** Aplicar `$base = max(0, $base);` antes de calcular el impuesto en los 4 archivos, y rechazar descuentos mayores al subtotal (`$desc_auto_amt = min($desc_auto_amt, $subtotal_lineas - $cupon_amt)`).

### ⚪ BAJO — `modules/ventas/guardar.php:63`
**guardar.php acepta es_extra sin gate de plan Business ni permiso agregar_extras: bypass del endpoint dedicado**

- **Evidencia:** `$es_extra = (int)($l['es_extra'] ?? 0);` se inserta tal cual (63-69) con solo el permiso de línea 12 (`eliminar_items_venta` o `editar_cotizaciones`). En cambio agregar_extra.php:12-16 exige `$plan_ext['es_business']` Y `agregar_extras`: `if (!$plan_ext['es_business']) json_error('Función disponible en plan Business', 403)`. Lo mismo pasa en cotizaciones/guardar.php:139 (`'es_extra' => (int)($item['es_extra'] ?? 0)`) sin gate.
- **Impacto:** Una empresa plan Free/Pro (o un asesor sin permiso agregar_extras) marca es_extra=1 en el payload del batch-save y obtiene la funcionalidad de extras que se vende como diferenciador Business — el gate de monetización solo protege el botón, no el dato.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código real. ventas/guardar.php:12 solo exige es_admin/eliminar_items_venta/editar_cotizaciones y en la línea 63-69 inserta es_extra=(int)($l['es_extra']??0) tal cual, SIN gate de plan. En contraste, ventas/agregar_extra.php:13-16 exige $plan_ext['es_business'] Y Auth::puede('agregar_extras'). Lo mismo en cotizaciones/guardar.php:15 (solo editar_cotizaciones) → líneas 139/189 insertan es_extra sin gate. Además public/cotizacion.php:80-83,840 renderiza la sección de extras basándose únicamente en es_extra=1, sin gate de plan. Escenario mínimo que lo dispara: un admin (o asesor con editar_cotizaciones) de una empresa Free/Pro envía un POST a /ventas/:id/guardar (o /cotizaciones/:id) con una línea del payload JSON conteniendo \"es_extra\":1; se persiste y el slug público lo muestra como \"extra\" (diferenciador Business) sin la validación es_business. El gate de monetización protege solo el endpoint dedicado y el botón del UI, no el dato en el batch-save. Ajusto a severidad baja: es un bypass de gating de monetización (fuga de feature de pago), pero NO hay acceso cross-tenant (todo scoped por empresa_id), ni daño a datos ni a integridad, y requiere fabricar deliberadamente el payload porque el UI oculta los controles de extras para no-Business.
- **Fix:** En ventas/guardar.php y cotizaciones/guardar.php: si `!trial_info(EMPRESA_ID)['es_business']` o el usuario no tiene agregar_extras, forzar `$es_extra = 0` (o preservar el valor previo de la línea).


---

## Motor Radar

### 🟠 ALTO — `modules/radar/Radar.php:1886`
**calibrar() entrena el FIT con sesiones contaminadas (internos/bots/sin dedupe) y con gap distinto al que usa score()**

- **Evidencia:** La query de calibración: "COUNT(qs.id) AS num_sess, COUNT(DISTINCT qs.ip) AS num_ips, DATEDIFF(MAX(qs.created_at), MIN(qs.created_at)) AS span_d FROM cotizaciones c LEFT JOIN quote_sessions qs ..." — sin `qs.es_interno=0`, sin filtro de bots/ghosts y sin deduplicación de 30 min. score() sí deduplica ($dedupe, línea 502-509), excluye internos y define gap como penúltima→última sesión (líneas 716-720). Además el header del archivo (línea 14) afirma "v2.3: Calibración gap: últimas 2 sesiones (no span total)" pero el código usa span total.
- **Impacto:** Las tasas rate_sess/rate_ips/rate_gap se aprenden sobre conteos inflados (sesiones del asesor pre-limpieza, cargas duplicadas, ghosts) y sobre un feature de gap distinto al que se evalúa en producción. El fit_pct resultante decide buckets enteros (prediccion_alta exige FIT≥14%, inminente FIT≥5%): empresas con mucho tráfico interno obtienen FIT sistemáticamente desplazado y el Radar clasifica mal en silencio cada 90 días de recalibración automática.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo Radar.php. Escenario mínimo: empresa calibrada donde un asesor abrió sus cotizaciones antes de ser detectado (filas quedan en quote_sessions con es_interno=1 vía layout.php:90, que marca sin borrar). calibrar() (líneas 1882-1892) las cuenta igual — sin es_interno=0, sin bot_ip/bot_ua, sin filtro behavioral ni dedupe — mientras score() (443-509) sí filtra y deduplica antes de llamar fit_prob() (766). Una cotización cerrada con 3 vistas del asesor + 2 del cliente entrena el bucket '5-7' cuando en producción se evaluaría como '2' → rate_sess desplazada. Peor aún, universal a toda empresa calibrada: el gap de entrenamiento es span total DATEDIFF(MAX,MIN) (línea 1886) pero score() evalúa gap penúltima→última sesión (715-720) — poblaciones distintas para el mismo bucket bk_gap ('sin' entrenado = todas las sesiones en 0 días; 'sin' evaluado = regresó el mismo día). El header línea 14 ("Calibración gap: últimas 2 sesiones, no span total") contradice el código, confirmando que es bug y no diseño. fit_pct gatea buckets reales: imminent_min_fit_pct=5 (línea 854) y predict_min_fit_pct=14 (línea 910). Atenuantes: Laplace α=5 + isotonic amortiguan, y la contaminación en cerradas/abiertas se cancela parcialmente en los ratios — degrada calidad del modelo en silencio, no corrompe datos ni crashea.
- **Fix:** En la query de calibrar(): agregar `AND qs.es_interno = 0`, deduplicar sesiones con la misma ventana que score() (subquery o agregación por ventana de 30 min), y calcular el gap penúltima→última sesión en vez de DATEDIFF(MAX,MIN).

### 🟡 MEDIO — `modules/radar/index.php:1260`
**Config Radar promete 'IPs internas (excluidas del radar)' pero nada las excluye, y los botones Agregar/Quitar pegan a rutas inexistentes**

- **Evidencia:** El tab Config muestra "IPs internas (excluidas del radar)" y el toggle "Excluir internos — Ignora visitas de IPs internas y del equipo" (líneas 1245-1248). Pero Radar.php solo INSERTA en radar_ips_internas (aprender_ip_radar, línea 1846) y nunca la lee en score(); el comentario de score() (líneas 370-373) lo confirma: "Ni la IP ni el device_sig se usan como señal de interno". public/cotizacion.php tampoco consulta radar_ips_internas (grep sin matches). Además el JS addIp()/delIp() (index.php:1346,1354) hace fetch a '/radar/ip' y DELETE '/radar/ip/'+id — Router.php solo registra GET /radar y POST /radar/descartar-comp: ambas rutas dan 404.
- **Impacto:** Un admin que agrega la IP de su oficina esperando que las vistas del equipo dejen de contar (asesores en incógnito/sin cookie) recibe 'Error' (404) o, vía /config/ip-interna, un registro que no filtra nada: las visitas internas siguen entrando al Radar como cliente. El cliente cree estar protegido y sus buckets se contaminan igual — exactamente el tipo de mentira que erosiona la confianza en el Radar.
- **Escenario que lo dispara (verificado):** Confirmado en código. Escenario mínimo: (1) admin abre Radar > Config, escribe una IP y pulsa "Agregar" → el JS (modules/radar/index.php:1354) hace POST a '/radar/ip', ruta que no existe en core/Router.php (solo hay GET /radar, POST /radar/descartar-comp, POST /config/ip-interna) → 404 → alert('Error'); el botón "✕" quitar (línea 1346) hace DELETE '/radar/ip/'+id — el Router ni siquiera soporta DELETE → 404 y r.json() lanza excepción sin feedback. (2) Aunque la IP entre por la ruta real /config/ip-interna, radar_ips_internas nunca se lee para filtrar: cotizacion.php:252-255 dice explícitamente "CAPA 2 (IP interna) eliminada" y Radar::score() (Radar.php:369-373) confirma "Ni la IP ni el device_sig se usan como señal de interno" — el toggle excluir_internos filtra solo por visitor_id/dsig. La cabecera "IPs internas (excluidas del radar)" (index.php:1260) y el subtítulo del toggle (1246) prometen una exclusión que no existe: un asesor en incógnito/sin cookie desde esa IP sigue contando como cliente. Severidad ajustada a medio (no alto): la eliminación de Capa 2 fue decisión deliberada y correcta (IPs rotan, Capas 0/1 hacen el trabajo real); el defecto agudo es UI engañosa + 2 botones muertos con 404, no una regresión de filtrado ni corrupción de datos.
- **Fix:** Decidir: (a) reactivar exclusión por IP en score()/cotizacion.php con TTL corto, o (b) eliminar la sección y el toggle engañoso de la UI. En cualquier caso, registrar las rutas /radar/ip (POST) y /radar/ip/:id (DELETE) o apuntar el JS a /config/ip-interna.

### 🟡 MEDIO — `modules/radar/Radar.php:588`
**El loop post-guest usa filtros viejos distintos al loop principal: ghost-restore infla multi_persona y probable_cierre**

- **Evidencia:** Loop post-guest: `if ($vi2 > 0 && $vi2 < 2000 && $sc2 === 0) continue;` (línea 588, comentado "paridad con loop principal") y ghost check por IP con CUALQUIER evento: `foreach ($ev_rows as $ev) { if (($ev['ip'] ?? '') === $ip2) { $ip2_has_ev = true; ... } }` (línea 584). El loop principal usa el filtro nuevo `$js_tocado && $vis < 200 && $scroll < 35` (línea 492) y ghost check por visitor_id exigiendo engagement real `$ev_scroll > 0 || $ev_vis >= 2000` (líneas 469-474). No hay paridad.
- **Impacto:** Las sesiones ghost-restore validadas con datos (scroll 20-34, visible<200ms — commit 3cbdeb0, '0 aceptadas y 9 activas' en la zona muerta) se excluyen de $sessions pero SÍ suman a $ips_post_guest/$vids_post_guest → activan multi_persona (línea 977) y la categoría social de probable_cierre (línea 1127), buckets con push. A la inversa, sesiones legítimas de vistazo rápido (visible 200-1999ms, scroll 0) que el loop principal acepta son excluidas del conteo multi-persona. El mismo cliente con una restauración de scroll de Chrome Android aparece como 'varias personas evaluando'.
- **Escenario que lo dispara (verificado):** Confirmado en código: Radar.php:588 usa el filtro viejo (vi2>0 && vi2<2000 && sc2===0) y :582-585 usa ghost check por IP con cualquier evento, mientras el loop principal usa js_tocado && vis<200 && scroll<35 (:492) y ghost check por vid con engagement real (:469-474) — los comentarios 'paridad con loop principal' son obsoletos. Escenario mínimo que lo dispara: cliente abre la cotización en el in-app browser de WhatsApp (vid A, sesión legítima → primer guest), luego toca 'abrir en Chrome' y Chrome Android restaura el scroll de la carga previa → sesión ghost con vid B, scroll_max=30, visible_ms=150. El loop principal la descarta de $sessions (30<35, 150<200) pero el loop post-guest NO la filtra (sc2=30≠0 esquiva ambos checks viejos) → vids_post_guest_count=2 → dispara multi_persona (línea 975, bucket caliente con push) y la categoría social de probable_cierre (línea 1127). El union-find F2 no lo corrige porque vid B no comparte dsig+IP con vid A. Asimetría inversa también confirmada: sesión legítima con visible=250ms/scroll=0 cuenta en $sessions pero la línea 588 la excluye del conteo multi-persona. Severidad ajustada a medio: el fallo es real y contamina buckets con push, pero requiere sesión ghost con vid/IP distinta dentro de la ventana multip post primer guest — frecuencia moderada según los datos históricos (~18 sesiones en zona muerta en 60 días).
- **Fix:** Replicar exactamente los filtros del loop principal en el loop post-guest: min-visita `($s['visible_ms'] !== null && $vi2 < 200 && $sc2 < 35)` y ghost check por vid con engagement (scroll>0 || vis>=2000).

### 🟡 MEDIO — `modules/radar/Radar.php:976`
**multi_persona y cat_social se disparan con e_uniq_v, métrica que el propio código declara inflada por vids fantasma**

- **Evidencia:** multi_persona: `$vids_post_guest_count >= 2 || $e_uniq_v >= 2 || $ips_post_guest_count >= ...` (línea 976) y cat_social de probable_cierre usa `$e_uniq_v >= 2` (línea 1126). El comentario de las líneas 783-784 dice textualmente: "usar solo vids de sesiones (más confiable que eventos JS) — e_uniq_v puede estar inflado por vids fantasma de antes del fix de PHP" y por eso el booster de priority ya lo abandonó. e_uniq_v tampoco pasa por el union-find de IP compartida (líneas 610-630) que resuelve el caso WhatsApp→Safari (nueva cookie, misma IP, misma persona) cuando los eventos no traen device_sig.
- **Impacto:** Un solo cliente que abre el slug en el in-app browser de WhatsApp/Messenger y luego en Safari genera 2 visitor_ids en quote_events; si los eventos no traen dsig (adblocker parcial, eventos previos al set de la cookie), e_uniq_v=2 activa 'Multi-persona' → bucket caliente, entra al resumen 'Probable cierre' y dispara push de 'decisión compartida en curso' para una sola persona. Es el 40% del tráfico (Messenger) según los propios datos del proyecto.
- **Escenario que lo dispara (verificado):** Confirmado: Radar.php:976 usa `$e_uniq_v >= 2` como trigger OR independiente de multi_persona (bypasea el conteo validado por union-find $vids_post_guest_count de líneas 610-630) y Radar.php:1126 lo usa en cat_social que alimenta probable_cierre; el comentario en 783-784 dice textualmente que e_uniq_v "puede estar inflado por vids fantasma de antes del fix de PHP" y el booster de priority ya lo abandonó. Escenario mínimo que lo dispara: cotización con 2+ guest sessions y actividad reciente cuyos quote_events (lookback 150 días, línea 348 — cubre eventos fantasma pre-fix del 19 mayo hasta ~octubre 2026, porque el ghost cleanup borra quote_sessions pero NO quote_events) contienen 2 visitor_ids donde al menos uno carece de device_sig en sus eventos (eventos previos al despliegue del dsig, getDeviceSig() con excepción, o fallback NULL AS device_sig de línea 361) → e_uniq_v=2 → multi_persona aunque el union-find de sesiones consolide a 1 persona → bucket caliente + probable_cierre + posible push para una sola persona. MATIZ que baja la severidad de alto a medio: el hallazgo exagera el escenario WhatsApp→Safari/Chrome del 40% de tráfico Messenger — _agregar_eventos (líneas 1296-1301) SÍ deduplica uniq_v por device_sig cuando los eventos lo traen, y el JS actual (cotizacion.php:1647) manda dsig en TODOS los eventos calculado síncronamente; mismo teléfono = mismo dsig → uniq_v=1 en el flujo presente. La exposición real es la población decreciente de eventos históricos sin dsig dentro del lookback más fallos raros de cómputo del dsig, no el tráfico in-app cotidiano.
- **Fix:** Quitar `$e_uniq_v >= 2` de multi_persona (línea 976) y de cat_social (líneas 1126-1127), dejando $vids_post_guest_count (validado por union-find) e IPs validadas como únicas señales, igual que ya se hizo con el booster de priority.

### 🟡 MEDIO — `modules/radar/Radar.php:1560`
**recalcular_empresa corta a 120s con orden fijo: la cola de cotizaciones nunca se recalcula y sus buckets calientes quedan pegados**

- **Evidencia:** `foreach ($cots as $c) { self::recalcular(...); $count++; if (time() - $start > 120) break; }` sobre una lista SIEMPRE ordenada `ORDER BY ultima_vista_at DESC, id DESC` (línea 1554). En index.php:48-53 el trigger de recálculo compara `MAX(radar_updated_at)` de TODA la empresa contra 5 min: como la cabeza de la lista se actualiza en cada corrida parcial, el MAX siempre es fresco y las cotizaciones de la cola (las más viejas, justo las que deberían decaer a enfriandose/NULL) no se recalculan jamás.
- **Impacto:** En una empresa con suficientes cotizaciones activas para exceder los 120s, una cotización que fue 'inminente' hace 3 semanas sigue mostrando ese badge y ese score en el Radar y en la lista de cotizaciones indefinidamente — el asesor persigue leads muertos creyendo en el Radar. Además las transiciones down nunca se escriben para esas cots, sesgando el Termómetro (Radar Health).
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Radar.php:1554-1560: recalcular_empresa ordena SIEMPRE por ultima_vista_at DESC (orden estable entre corridas — solo cambia con visitas de clientes) y corta con `if (time()-$start > 120) break`. Cada re-trigger (radar/index.php:53-54 cada 5 min, dashboard/index.php:381) recorre la MISMA cabeza y vuelve a agotar los 120s antes de llegar a la cola. No hay ruta de rescate: Radar::recalcular() individual solo se llama desde track.php:311 y cotizacion.php:316 (requieren visita del cliente — un lead muerto no visita); no existe cron de radar; auto_suspender está DEFAULT 0 (add_suspender.sql:13) y aun activo no cubre estado 'aceptada', cuyo conjunto crece sin tope en el query set. Las transiciones down solo se escriben dentro de recalcular(), así que el sesgo al Termómetro también es real. Escenario mínimo que lo dispara: empresa con auto_suspender apagado (default) y suficientes cotizaciones activas (enviada/vista/aceptada no suspendidas, ~400-1200+ según latencia por recalc de ~100-300ms) para que la cabeza consuma >120s en cada corrida; una cotización con ultima_vista_at viejo y radar_bucket='inminente' queda congelada indefinidamente con badge y score obsoletos. Matiz menor: la frase "el MAX siempre es fresco" del hallazgo es imprecisa (el recálculo SÍ se re-dispara tras 5 min), pero la consecuencia descrita —starvation de la cola por orden fijo + timeout— es exactamente la que produce el código. Severidad medio es honesta: bug de diseño real pero condicionado a escala; los tenants actuales probablemente no exceden 120s todavía, aunque el crecimiento de aceptadas lo vuelve inevitable en tenants exitosos.
- **Fix:** Recorrer la cola con rotación: `ORDER BY radar_updated_at ASC` (las más viejas primero) o persistir un cursor; alternativamente mover el recálculo masivo a cron y dejar el on-page solo para la cabeza.

### 🟡 MEDIO — `modules/radar/index.php:64`
**Para el asesor filtrado, 'Cierre global' divide ventas de TODA la empresa entre sus cotizaciones propias**

- **Evidencia:** `$uw = $uid_filtro ? "AND c.vendedor_id=" . intval($uid_filtro) : ''` se aplica a $stat_total (línea 59-63) pero $stat_aceptadas es `SELECT COUNT(*) FROM ventas WHERE empresa_id=? AND estado != 'cancelada'` (líneas 64-68) SIN filtro de vendedor. $stat_cierre = aceptadas/total (línea 69) se muestra como 'Cierre global: X%' y la tarjeta '✅ Aceptadas'.
- **Impacto:** Un asesor sin permiso ver_todas_cots con 10 cotizaciones en una empresa con 50 ventas ve 'Cierre global: 500%' y 'Aceptadas: 50' como si fueran suyas. Métrica matemáticamente imposible visible cada vez que abre el Radar — mina la credibilidad de todos los demás números del módulo.
- **Escenario que lo dispara (verificado):** Confirmado en modules/radar/index.php: con un usuario no-admin sin permiso 'ver_todas_cots' (línea 34, $uid_filtro=Auth::id()), $stat_total (líneas 59-63) se filtra con "AND c.vendedor_id=<uid>" pero $stat_aceptadas (líneas 64-68) cuenta TODAS las ventas no canceladas de la empresa sin filtro de vendedor. $stat_cierre (línea 69) divide ventas globales entre cotizaciones propias. Escenario mínimo: asesor con permisos restringidos y 10 cotizaciones (no borrador) en una empresa con 50 ventas activas abre /radar → cabecera (línea 712) muestra "Aceptadas: 50 · Cierre global: 500%" y las tarjetas (líneas 942, 946) muestran "✅ Aceptadas: 50" y "📊 Tasa cierre: 500%". Sin ningún condicional de rol en esos bloques de UI. Severidad medio es honesta: es un bug de visualización (numerador global / denominador personal) que produce métricas imposibles y engañosas para asesores, pero no corrompe datos ni afecta buckets, termómetro ni decisiones automatizadas.
- **Fix:** Aplicar el mismo filtro de vendedor a $stat_aceptadas (JOIN ventas→cotizaciones filtrando COALESCE(c.vendedor_id,c.usuario_id)=uid) o etiquetar las tarjetas como métricas de empresa cuando hay filtro.

### 🟡 MEDIO — `modules/radar/Radar.php:2181`
**engage_avg() calcula el umbral de 'lectura_comprometida' incluyendo sesiones internas**

- **Evidencia:** "SELECT ROUND(AVG(qs.scroll_max)), ROUND(AVG(qs.visible_ms)) ... FROM quote_sessions qs JOIN cotizaciones c ... JOIN ventas v ... WHERE v.empresa_id = ? AND v.estado != 'cancelada' AND qs.scroll_max > 0 AND qs.visible_ms > 0" — sin `qs.es_interno = 0` ni exclusión por radar_visitors_internos. Las sesiones del asesor previsualizando sus propias cotizaciones vendidas (caso documentado: botón 'Ver' del editor) entran al promedio.
- **Impacto:** El bucket lectura_comprometida exige `$e_scroll_any >= $ea['scroll'] && $e_vis_max >= $ea['vis_ms']` (líneas 931-933). Asesores que revisan a fondo sus cotizaciones antes de vender inflan el promedio → clientes reales con lectura buena pero menor al umbral inflado dejan de aparecer en un bucket caliente que dispara push. El auto-calibrado 'con N ventas' que presume la UI está entrenado con datos del propio equipo.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Radar.php:2177-2185 (engage_avg) promedia scroll_max/visible_ms de quote_sessions JOIN ventas sin filtro `qs.es_interno = 0` ni exclusión por radar_visitors_internos ('es_interno' no aparece en todo Radar.php). Las sesiones internas con engagement real SÍ existen en la tabla: core/layout.php:85-93 marca retroactivamente `es_interno=1` las sesiones fugadas del asesor pero conserva las filas con su scroll/visible (casos documentados: botón 'Ver' en dominio custom — Kevin, Manuel, superadmin). La asimetría es real: los valores comparados ($e_scroll_any/$e_vis_max, líneas 730/732) vienen de _agregar_eventos() que SÍ excluye internos (línea 1246), pero el umbral no. Escenario mínimo que lo dispara: empresa con ≥5 ventas (línea 2191 activa modo auto); un asesor abre el slug de su propia cotización con el botón 'Ver' desde un dominio custom sin cookies → se crea quote_session con scroll ~100% y visible alto; el cleanup la marca es_interno=1 pero la fila queda; la cotización se convierte en venta → engage_avg la promedia y el umbral sube (ej. de 70 a 78); un cliente real con scroll 72 y buena lectura deja de calificar para lectura_comprometida (líneas 926-937) y no dispara el bucket caliente. Severidad medio es honesta: los clamps (scroll 50-95, vis 10s-180s) acotan la deriva, el sesgo puede ir en ambas direcciones, afecta solo 1 bucket de varios calientes, y solo contaminan las sesiones fugadas-luego-limpiadas en cotizaciones que llegaron a venta (los internos detectados nunca insertan sesión por skip_tracking).
- **Fix:** Agregar `AND qs.es_interno = 0 AND qs.visitor_id NOT IN (SELECT visitor_id FROM radar_visitors_internos WHERE empresa_id = ?)` a la query de engage_avg().

### 🟡 MEDIO — `api/cot_feedback.php:68`
**cot_feedback: el bloqueo de internos solo aplica si el cliente envía visitor_id — un asesor en incógnito puede autocalificarse 5 estrellas**

- **Evidencia:** `if (!$es_superadmin_fb && $visitor_id) { $vid_interno = ... }` — el check de radar_visitors_internos solo corre cuando $visitor_id viene en el body, y $visitor_id lo manda el propio cliente (línea 17). Sin cookie cz_vid (incógnito, primer visita) el POST pasa sin ninguna verificación de identidad; tampoco se comprueba Auth::logueado() ni sesión del asesor no-superadmin.
- **Impacto:** Cualquier asesor abre su slug en incógnito y registra una calificación de 5 estrellas con comentario 'excelente atención' que llega al reporte de feedback y dispara push a la empresa como si fuera el cliente. En empresas Business con leaderboard/reportes por vendedor, las calificaciones dejan de ser confiables (un dato falso presentado como voz del cliente).
- **Escenario que lo dispara (verificado):** Confirmado leyendo api/cot_feedback.php y core/Router.php:87. La ruta /api/cot-feedback es pública (self::post sin gate de login/CSRF) y el endpoint no llama Auth::logueado() ni csrf_check(). El único guard de identidad es el bloque de la línea 68, que es doblemente condicional: `if (!$es_superadmin_fb && $visitor_id)` y dentro consulta radar_visitors_internos. Escenario mínimo que lo dispara: un asesor abre su propio slug en incógnito (sin cookie cz_vid) sobre una cotización aún NO calificada de su empresa con feedback_activo=1, y hace POST con stars=5 + comentario. Como en incógnito no hay cz_vid, el JS o no manda visitor_id (check de línea 68 se salta entero) o manda un vid nuevo que nunca fue registrado como interno (la subconsulta a radar_visitors_internos no devuelve fila y pasa). El INSERT graba la calificación con vendedor_id tomado de la propia cotización (línea 81) y las líneas 102-129 disparan el push a la empresa como si fuera el cliente. La única barrera real es el check de "ya_calificado" (línea 46), que limita a 1 reseña falsa por cotización, y requiere conocer el slug — por eso severidad media/baja, no crítica: es un problema de integridad de datos del feedback/leaderboard, no de compromiso del sistema.
- **Fix:** Verificar también la sesión del request: si Auth::logueado() y el usuario pertenece a la empresa (no solo superadmin), rechazar con 'interno'. Mantener el check por vid como capa adicional, no única.

### ⚪ BAJO — `modules/radar/Radar.php:1529`
**Dedupe de push por LIKE sin delimitador: la cotización 12 suprime su alerta si la 123 tuvo push en 24h**

- **Evidencia:** `AND datos LIKE ?` con parámetro `'%"cotizacion_id":' . $cotizacion_id . '%'` (líneas 1526-1529). En el JSON almacenado el valor va seguido de coma o llave (`"cotizacion_id":123,`), así que el patrón de la cot 12 (`%"cotizacion_id":12%`) matchea los registros de las cots 120-129, 1234, etc.
- **Impacto:** Cuando la cotización 12 entra a un bucket caliente, el sistema encuentra el push de la cot 123 enviado hace 3 horas y suprime la alerta. El asesor pierde la notificación de un probable cierre real — exactamente la señal por la que paga el Radar. La probabilidad crece con el volumen de cotizaciones (IDs de 4 dígitos ya en producción: 3948, 4038…).
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código: Radar.php:1541 envía datos=['cotizacion_id'=>$id,...] y PushNotification.php:154 lo persiste con json_encode → el campo datos contiene '"cotizacion_id":123,'. El dedupe en Radar.php:1525-1529 usa LIKE con patrón '%"cotizacion_id":12%' sin delimitador de cierre (coma/llave), por lo que el registro de la cot 123 matchea el patrón de la cot 12. Escenario mínimo: empresa X tiene activas las cotizaciones 12 y 123; la cot 123 entró a un bucket caliente hace 3h y recibió push radar_* (row en notificaciones_push con datos {"cotizacion_id":123,...}); luego la cot 12 cambia a un bucket de PUSH_BUCKETS → el COUNT devuelve ≥1 → $ya_enviado>0 → el push de la cot 12 se suprime silenciosamente. Severidad ajustada a BAJO (no medio): el patrón solo cruza IDs donde uno es prefijo estricto del otro (longitudes distintas, p.ej. 394 vs 3948), y con IDs secuenciales la cot de ID corto es meses más vieja que la larga; que ambas estén activas, en la misma empresa, y coincidan en la ventana de 24h es infrecuente. El fix correcto es incluir el delimitador en el patrón ('%"cotizacion_id":'.$id.',%' o usar JSON_EXTRACT).
- **Fix:** Cambiar el patrón a `'%"cotizacion_id":' . $cotizacion_id . ',%'` (o mejor: `JSON_EXTRACT(datos, '$.cotizacion_id') = ?`), o agregar columna cotizacion_id indexada a notificaciones_push.

### ⚪ BAJO — `modules/radar/index.php:752`
**El detalle de la alerta de competencia no filtra es_interno=0: muestra sesiones del asesor como vistas del 'competidor'**

- **Evidencia:** render_comp_row consulta "FROM quote_sessions qs JOIN cotizaciones c ... WHERE {$where_main} = ? AND c.empresa_id = ? AND qs.created_at >= DATE_SUB(NOW(), INTERVAL 180 DAY) AND (qs.visible_ms > 3000 OR qs.scroll_max > 10)" (líneas 741-758) — sin `qs.es_interno = 0`. La query que genera la alerta (comp_by_user, línea 858) SÍ filtra `qs.es_interno = 0`, pero el detalle expandido no.
- **Impacto:** El listado 'qué clientes y cotizaciones vio este visitante' puede incluir sesiones marcadas internas retroactivamente (limpiezas), inflando el número de cotizaciones/clientes mostrados en rojo. El asesor confronta una amenaza de competencia dimensionada con datos que el propio sistema ya anuló — falso alarmismo en la alerta más sensible del Radar.
- **Escenario que lo dispara (verificado):** Confirmado en modules/radar/index.php: la query de render_comp_row (líneas 741-758) no incluye `qs.es_interno = 0` mientras que comp_by_user (línea 858) sí lo filtra. Escenario mínimo: un visitor_id con 2+ clientes vistos en sesiones es_interno=0 dispara la alerta; si ese mismo visitor tiene otras sesiones marcadas es_interno=1 retroactivamente (limpiezas por IP documentadas en el proyecto, que marcan sesiones sin registrar el vid en radar_visitors_internos), el detalle expandido las lista igual — mostrando más clientes/cotizaciones/IPs de los que el badge (correcto, filtrado) reporta. Matiz que baja la severidad: el disparo de la alerta y los contadores del encabezado (clientes_distintos, cots_vistas) provienen de la query filtrada, así que la alerta en sí es legítima; solo las filas del detalle expandido pueden incluir sesiones anuladas, y el escenario requiere sesiones del mismo visitor_id anuladas por IP sin que el vid sea interno.
- **Fix:** Agregar `AND qs.es_interno = 0` al WHERE de la query en render_comp_row (línea ~753).

### ⚪ BAJO — `modules/radar/Radar.php:547`
**no_abierta ignora la vigencia: $vigencia_ts se calcula y nunca se usa, aunque la UI promete 'dentro de su vigencia'**

- **Evidencia:** `$vigencia_ts = ($cot_meta_early && $cot_meta_early['valida_hasta']) ? strtotime($cot_meta_early['valida_hasta']) : $created_early + 30 * 86400;` — única aparición de la variable en el archivo (verificado con grep); la condición del bucket (línea 551) solo evalúa `!$ya_abierta && $no_abierta_age_ok && !$has_js`. El hint del bucket en index.php:1078 dice "con más de 24h y dentro de su vigencia".
- **Impacto:** Cotizaciones vencidas hace meses siguen listadas como '❌ No abierta' en el Radar, ensuciando el bucket con leads muertos y contradiciendo la descripción visible al asesor. El asesor pierde tiempo persiguiendo cotizaciones expiradas o deja de confiar en la lista.
- **Fix:** Usar la variable ya calculada: agregar `&& $now <= $vigencia_ts` a la condición de no_abierta (o quitar 'dentro de su vigencia' del hint si se decide mostrarlas con etiqueta VENCIDA).

### ⚪ BAJO — `modules/radar/index.php:233`
**Score% fabricado cuando falta fit_pct: la UI inventa score×0.65 y lo presenta con 1 decimal**

- **Evidencia:** `'fit_pct' => (float)($senales['fit_pct'] ?? max(0,min(100,(float)$score*0.65)))` — cuando radar_senales no trae fit_pct (registros pre-migración o JSON truncado), la columna 'Score%' muestra un número derivado del priority multiplicado por una constante arbitraria, en negritas y con formato de porcentaje real (línea 407).
- **Impacto:** El asesor compara cotizaciones por un 'Score%' que en algunas filas es la probabilidad FIT calibrada y en otras es un invento (score×0.65). Dos cotizaciones idénticas pueden mostrar Score% distinto solo por la antigüedad de su JSON — dato falso presentado con precisión decimal.
- **Fix:** Mostrar '—' cuando fit_pct no existe en radar_senales en lugar de fabricar el valor: `'fit_pct' => isset($senales['fit_pct']) ? (float)$senales['fit_pct'] : null` y renderizar null como guion.


---

## Tracking / Escudo

### 🟠 ALTO — `public/cotizacion_inmueble.php:604`
**Giro inmuebles: el tracker JS envía 'scroll_max' pero track.php lee 'max_scroll' — el scroll llega siempre 0**

- **Evidencia:** cotizacion_inmueble.php:604: `var d={cotizacion_id:COT,empresa_id:EID,tipo:tipo,visible_ms:visibleAccum,scroll_max:maxScroll,visitor_id:VID};` — la clave es `scroll_max`. api/track.php:25 lee otra clave: `$max_scroll = min(100, max(0, (int)($data['max_scroll'] ?? 0)));` → siempre 0. En cambio el template de servicios (cotizacion.php:1650) sí manda `max_scroll: maxScroll`. Además el payload inmueble omite device_sig, session_id, page_id y open_ms.
- **Impacto:** Para TODO el giro inmuebles, quote_sessions.scroll_max y quote_events.max_scroll quedan en 0 aunque el cliente scrollee toda la propiedad. El Radar pierde las señales de scroll (probable_cierre requiere scroll 70%+), los milestones quote_scroll llegan con scroll 0, y el filtro de Radar `vis<200 && scroll<35` (Radar.php:492) descarta sesiones reales de lectura breve. El asesor inmobiliario ve 'cliente no leyó' cuando sí leyó — el activo #1 (Radar confiable) miente sistemáticamente para un giro completo.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código: cotizacion_inmueble.php:604 envía la clave 'scroll_max' pero api/track.php:25 lee $data['max_scroll'] (con default 0), mientras que el template de servicios (cotizacion.php:1650) sí envía 'max_scroll'. Escenario mínimo: cliente abre un slug giro=inmuebles y scrollea al 100% — el JS manda quote_scroll/quote_close con scroll_max:100, track.php parsea max_scroll=0, y quote_sessions.scroll_max (línea 158, GREATEST con 0) y quote_events.max_scroll (línea 179-181) quedan en 0 para siempre. Consecuencias verificadas: buckets que exigen scroll 70%+ (probable_cierre) inalcanzables para todo el giro inmuebles, milestones quote_scroll persistidos con scroll 0, y el filtro vis<200 && scroll<35 puede descartar lecturas breves reales. También confirmado que el payload inmueble omite device_sig, session_id, page_id y open_ms. Severidad ajustada a ALTO (no crítico) porque visible_ms sí llega con la clave correcta — el Radar conserva la señal de tiempo visible y las visitas no se pierden ni marcan 'sin abrir'; lo roto es la dimensión scroll completa de un giro, no el tracking total.
- **Fix:** En cotizacion_inmueble.php:604 renombrar la clave a `max_scroll:maxScroll` y agregar `session_id`, `page_id`, `device_sig` y `open_ms` al payload, igual que el sendEvent() de cotizacion.php:1642-1654.

### 🟡 MEDIO — `modules/dashboard/index.php:353`
**Ghost cleanup del dashboard borra sesiones con visible_ms NULL (adblocker/JS no cargó) que track.php protege explícitamente**

- **Evidencia:** dashboard/index.php:352-355: `AND COALESCE(qs.scroll_max, 0) = 0 AND COALESCE(qs.visible_ms, 0) = 0 ... AND NOT EXISTS (SELECT 1 FROM quote_events qe WHERE qe.cotizacion_id = qs.cotizacion_id AND qe.ip = qs.ip)`. COALESCE(visible_ms,0)=0 incluye visible_ms IS NULL. track.php:287 hace lo contrario a propósito: `AND visible_ms IS NOT NULL` con comentario 'visible_ms IS NULL → cliente con adblocker... MANTENER: server-side ya registró visita legítima'.
- **Impacto:** Cliente real que abre desde WhatsApp/adblocker y cuyo JS nunca corre: no genera quote_events → cuando el asesor abre su dashboard 2+ min después, la sesión se borra y visitas se decrementa. Es la misma clase de falso negativo del caso Rodano (cot 4038): el asesor ve 0 visitas/historial vacío de un cliente que sí abrió. Los dos cleanups (dashboard vs track.php) aplican políticas contradictorias sobre los mismos datos.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código. Escenario mínimo: cliente real con adblocker (o browser donde el JS del tracker nunca corre) abre el slug → public/cotizacion.php:293 inserta quote_session SIN scroll_max/visible_ms (quedan NULL), cuenta visitas=1 y pone estado='vista'; como track.php nunca se ejecuta, no hay ningún quote_event con esa IP. Cuando el asesor abre el dashboard ≥2 minutos después, el query de dashboard/index.php:346-357 matchea la sesión (COALESCE(qs.visible_ms,0)=0 incluye NULL, y el NOT EXISTS sobre quote_events se cumple porque el JS jamás mandó eventos) → la sesión se BORRA y visitas se decrementa (queda en 0). Esto contradice exactamente la política de api/track.php:287-288, que excluye visible_ms IS NULL con el comentario explícito 'cliente con adblocker... MANTENER: server-side ya registró visita legítima'. MATIZ que baja la severidad respecto al hallazgo original: el cleanup del dashboard NO revierte estado ni vista_at (líneas 370-374 lo dicen expresamente), así que NO reproduce el caso Rodano completo — la cotización sigue como 'vista' y no aparece en 'sin abrir'; además ver.php ya oculta esas sesiones del historial visible. El daño real es: contador visitas=0 con estado='vista' (inconsistente), pérdida permanente de la quote_session de un cliente legítimo (afecta Radar y recálculos posteriores de visitas/vista_at), y dos cleanups con políticas contradictorias sobre los mismos datos.
- **Fix:** Alinear el criterio del dashboard con track.php: cambiar a `qs.visible_ms IS NOT NULL AND qs.visible_ms < 200 AND qs.scroll_max = 0` (o reutilizar la misma lógica en una función compartida).

### 🟡 MEDIO — `core/layout.php:104`
**Cleanup retroactivo de layout.php no recalcula vista_at ni revierte estado — la cotización queda 'vista' con la hora del asesor**

- **Evidencia:** layout.php:102-112 tras marcar sesiones es_interno=1 solo actualiza: `c.ultima_vista_at = (SELECT MAX(qs.created_at) ... es_interno = 0), c.visitas = (SELECT COUNT(*) ... es_interno = 0)`. No toca `vista_at` ni `estado`. Si la ÚNICA sesión era del asesor (leak), la cot conserva estado='vista' y vista_at con el timestamp del asesor, mientras ultima_vista_at queda NULL y visitas=0.
- **Impacto:** El asesor/dueño ve 'Vista hace X días' y el estado morado 'Vista' en una cotización que NINGÚN cliente abrió — decide no dar seguimiento porque cree que el cliente ya la vio. El termómetro cuenta esa cot en cot_vistas (Activación inflada). Es exactamente el pendiente documentado del 20 mayo ('sesión anulada deja fecha/hora sucia') pero para vista_at, que el fix de ultima_vista_at dejó fuera.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. Escenario mínimo: (1) un asesor abre el slug público desde un contexto donde las 3 capas fallan (ej. dominio custom sin cookie + IP nueva) → public/cotizacion.php:302 ejecuta `UPDATE cotizaciones SET estado='vista', vista_at=NOW() WHERE estado='enviada'` y crea la ÚNICA quote_session de esa cotización con es_interno=0. (2) Al día siguiente el asesor abre el dashboard → el cleanup de core/layout.php:85-93 marca la sesión es_interno=1, y el recalc de layout.php:102-112 SOLO actualiza `ultima_vista_at` (→NULL) y `visitas` (→0) — nunca toca `vista_at` ni `estado`. Resultado verificado: cotización con estado='vista', vista_at=hora del asesor, visitas=0, ultima_vista_at=NULL. Consecuencias confirmadas en código: modules/cotizaciones/ver.php:338-339 muestra "· Vista hace X" basado en vista_at (el asesor cree que el cliente la abrió y no da seguimiento), y core/ActividadScore.php:205-210 cuenta la cot en cot_vistas porque `estado IN ('vista',...)` sin importar visitas=0 (Activación inflada). No hay ruta que lo corrija: el recalc de vista_at en api/track.php:297-304 solo corre si se BORRAN sesiones ghost (scroll=0, vis<200) al llegar un evento nuevo — no cubre sesiones marcadas internas por layout.php — y nada revierte estado 'vista'→'enviada' (solo el re-envío manual en enviar.php:32). El propio comentario en layout.php:100 ("Sin abrir usa estado/vista_at... no hay riesgo de alarma falsa") reconoce que estado/vista_at gobiernan la señal, pero omite el falso positivo inverso: la cot queda marcada 'vista' sin que ningún cliente la abriera. Ajusto severidad a medio: el defecto es real y persistente en la señal central del producto, pero requiere como precondición un leak (las 3 capas fallando), que tras los fixes del Escudo de mayo es un evento raro; el daño por ocurrencia se limita a la cotización leakeada.
- **Fix:** En el mismo UPDATE agregar `c.vista_at = (SELECT MIN(qs.created_at) FROM quote_sessions qs WHERE qs.cotizacion_id=c.id AND qs.es_interno=0)` y, si el resultado es NULL y estado='vista', evaluar revertir a 'enviada' (o al menos marcarla para revisión).

### 🟡 MEDIO — `modules/radar/Radar.php:401`
**Radar::score() no filtra quote_sessions.es_interno=1 — sesiones marcadas internas siguen contando en los buckets si su vid no está en la lista**

- **Evidencia:** Radar.php:401-404: `SELECT ip, user_agent AS ua, visitor_id, device_sig, created_at, scroll_max, visible_ms FROM quote_sessions WHERE cotizacion_id=? AND created_at >= FROM_UNIXTIME(?)` — sin `AND es_interno=0`. El único descarte de internos es por lista (línea 444-447: `isset($intern_v[$vid])`). Lo mismo aplica a quote_events (línea 350-357, sin concepto de interno por sesión).
- **Impacto:** Toda sesión marcada es_interno=1 cuyo visitor_id NO esté en radar_visitors_internos (limpiezas manuales por SQL como las de cot 3973/3974 filtradas por IP, vids huérfanos de dominios custom pre-fix) sigue alimentando buckets: la cot puede quedar en 'probable_cierre' por visitas del asesor mientras el contador de visitas y el historial (que sí filtran es_interno=0) muestran 0. Superficies inconsistentes = Radar que se contradice a sí mismo frente al cliente que paga por él.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código: Radar.php:401-411 carga quote_sessions sin `AND es_interno=0` y el archivo completo no referencia es_interno (grep = 0 hits en Radar.php); el único descarte de internos es la lista vid/dsig en líneas 444-447. Escenario mínimo que lo dispara: asesor/superadmin abre un slug en dominio custom con cz_vid huérfano (nunca registrado en radar_visitors_internos porque Capa 1 es ciega ahí) → se crea quote_session es_interno=0 → se limpia con UPDATE manual por IP a es_interno=1 (práctica documentada del proyecto: cot 3973/3974, IP 187.245.114.71) → ver.php:78/track.php:299/layout.php:106 la excluyen (visitas=0, historial vacío) pero Radar::score() la sigue contando porque ni filtra es_interno ni encuentra el vid en la lista → la cot puede sostener bucket 'probable_cierre' con contador de visitas en 0. Severidad ajustada a MEDIO (no alto) porque el único path de CÓDIGO que setea es_interno=1 (layout.php:85-93) exige por INNER JOIN que el vid esté en radar_visitors_internos — en operación puramente por código ambos filtros quedan sincronizados y el vid-filter de score() sí excluye esas sesiones; la divergencia solo se materializa con marcados manuales por SQL de vids no registrados, caso real y documentado pero de bajo volumen.
- **Fix:** Agregar `AND es_interno = 0` al query de sesiones en score() (con fallback si la columna no existe) — coincide con el pendiente documentado 'es_interno=1 + capa_motivo: 5 queries downstream'.

### 🟡 MEDIO — `public/cotizacion.php:1632`
**getScrollPct() devuelve 100 cuando la página cabe en el viewport — quote_open reporta scroll=100 sin interacción y los previews evaden el filtro anti-ghost**

- **Evidencia:** cotizacion.php:1631-1633: `var den = sh - wh; if (den <= 0) return 100;` y en la init (línea 1807-1808) `updateMaxScroll(); sendEvent('quote_open', false);` — en una cotización corta que cabe en pantalla, el primer evento ya viaja con max_scroll=100 sin que el usuario tocara nada.
- **Impacto:** Los previews modernos que ejecutan JS <100ms (iMessage/Teams, documentados en el propio código) sobre cotizaciones cortas generan sesiones con scroll_max=100 y visible_ms bajo: el filtro del Radar `vis<200 && scroll<35` (Radar.php:492) NO las atrapa (scroll=100), el ghost cleanup tampoco (scroll>0). El preview cuenta como sesión con engagement máximo de scroll → bucket caliente falso en cotizaciones de 1-2 líneas.
- **Escenario que lo dispara (verificado):** CONFIRMADO. Escenario mínimo: cotización corta cuyo contenido cabe en el viewport del visitante (den = scrollHeight - innerHeight <= 0). Al cargar, la init (cotizacion.php:1807-1808) ejecuta updateMaxScroll() antes de sendEvent('quote_open') y getScrollPct() (línea 1632) devuelve 100 → quote_open viaja con max_scroll=100 sin interacción. track.php persiste scroll_max=100 en quote_sessions (líneas 139-141/158). Con eso la sesión evade los TRES filtros anti-ghost: behavioral de Radar.php:466 (exige scroll===0), visita mínima Radar.php:492 (exige scroll<35) y ghost cleanup track.php:286 (exige scroll_max=0). Agravantes verificados que el hallazgo no citó: (a) checkTotals() en la init (línea 1810) dispara section_view_totals inmediatamente porque los totales ya están en viewport — evento de engagement falso; (b) track.php:211 exige max_scroll>0 para actualizar ultima_vista_at → el ghost con scroll=100 también contamina esa fecha. La clase de previews con UA limpio que ejecutan JS existe empíricamente: está documentada en el propio Radar.php:455-458 como observada en producción (los clásicos iMessage/WhatsApp sí los para es_bot() por UA, pero los headless de oficina/Teams con UA normal pasan). Limitante honesta: requiere cotización que quepa completa en el viewport (corta + viewport alto de renderer desktop) y una sola sesión así no basta para probable_cierre (exige 15s+ de visibilidad), pero sí cuenta como sesión con engagement en score(), infla multi_persona/re_enganche y genera section_view_totals. Severidad medio es correcta.
- **Fix:** Cuando den<=0 devolver 0 y tratar 'página completa visible' como señal aparte, o solo reportar scroll>0 si hubo interacción real (`navigator.userActivation.hasBeenActive`), que además resuelve el scroll-restore de raíz.

### 🟡 MEDIO — `public/cotizacion.php:166`
**Parámetro _sv sin firma permite fijar el cz_vid de cualquier visitante (y ya no lo emite nadie: código huérfano)**

- **Evidencia:** cotizacion.php:166-183: `if (!empty($_GET['_sv'])) { $sync_vid = substr(preg_replace(...), $_GET['_sv']), 0, 64); ... setcookie('cz_vid', $sync_vid, ['expires' => time() + 730 * 86400, ...` — sin HMAC ni validación de origen (el bridge sí usa HMAC). Grep en todo el repo: ningún archivo genera links con _sv; solo este consumidor existe.
- **Impacto:** Cualquier URL `/c/slug?_sv=X` reescribe el visitor_id del cliente por 730 días. Quien conozca un vid interno puede anexarlo a un link y las visitas de ese cliente se descartan por Capa 1 (cliente invisible en el Radar); con vids arbitrarios se fusionan identidades de clientes distintos (multi_persona falso, dedupe cruzado). Es un setter de identidad sin autenticación en el pipeline cuya integridad es el producto.
- **Escenario que lo dispara (verificado):** CONFIRMADO. cotizacion.php:166-183 fija la cookie cz_vid (730 días) directamente desde $_GET['_sv'], sin HMAC ni verificación de origen — a diferencia del bridge legítimo (login_post.php:140-161) que firma con APP_SECRET vía /api/safari-bridge. Grep en todo el repo (.php y .js): _sv solo aparece como parámetro de entrada en cotizacion.php; NINGÚN archivo emite links con _sv → código huérfano (el redirect chain de login usa el token firmado 't=', no _sv). Escenario mínimo que lo dispara: enviar a la víctima /c/{slug}?_sv={vid}. Al abrirlo, el código sanitiza {vid} a [A-Za-z0-9-_]{0,64}, hace setcookie cz_vid=vid por 730 días + $_COOKIE['cz_vid']=vid, y redirige a la URL limpia. En la petición redirigida CAPA 1 (línea 247) evalúa Radar::es_visitor_interno con ese valor: si {vid} es un visitor_id interno conocido (el cz_vid es httponly=false, legible por JS y filtrable), la visita hace goto skip_tracking y el cliente queda invisible en el Radar por 730 días. Con vids arbitrarios (sin necesidad de conocer uno interno) se corrompe la deduplicación/identidad dentro de la cotización. Es un setter del primitivo de identidad sin autenticación en el pipeline cuya integridad es el producto. Severidad medio: el impacto grave (cliente invisible) exige conocer/filtrar un vid interno, lo que sube la barra; la parte trivial (fijar/fusionar vids arbitrarios) degrada señal del Radar pero no expone datos. Además es removible sin pérdida funcional por ser huérfano.
- **Fix:** Eliminar el bloque _sv completo (el bridge HMAC de safari_bridge.php ya cubre el caso de uso) o exigir firma HMAC igual que el bridge.

### 🟡 MEDIO — `public/cotizacion.php:1831`
**Los pixels de marketing cliente-side se disparan también para asesores internos y superadmin — solo el CAPI está gateado**

- **Evidencia:** cotizacion.php:1831 `<?= MarketingPixels::evento_view(EMPRESA_ID, $cot['numero'], (float)$total_base, ...) ?>` y :654 `scripts_base(EMPRESA_ID)` (fbq PageView) se imprimen en el HTML incondicionalmente; el `goto skip_tracking` de Capa 0 (línea 239) solo salta el tracking server-side. capi_view (línea 320) sí queda dentro del bloque protegido. Mismo patrón en cotizacion_inmueble.php:634.
- **Impacto:** Cada vez que un asesor previsualiza su cotización (botón 'Ver' del editor, revisiones del superadmin) Meta/GA4/TikTok reciben PageView + ViewContent con el valor de la cot. Las audiencias de retargeting del cliente Business se llenan de sus propios empleados y las métricas de campaña (base del módulo Marketing que se vende como diferenciador) quedan infladas — el empresario paga anuncios re-dirigidos a su propio equipo.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código: en public/cotizacion.php la Capa 0 (líneas 223-239) hace goto skip_tracking (label línea 332) que solo omite el tracking server-side — quote_session, visitas, Radar y capi_view (línea 320, dentro del bloque de cliente real). El render HTML continúa igual para internos: línea 654 imprime MarketingPixels::scripts_base(EMPRESA_ID) y línea 1831 MarketingPixels::evento_view(...), sin condición alguna sobre $es_usuario_interno. En core/MarketingPixels.php, scripts_base() y evento_view() solo checan marketing_config (no Auth ni es_bot) y emiten fbq('track','PageView'), fbq('track','ViewContent',{value:...}), gtag y ttq. Mismo patrón en cotizacion_inmueble.php:175/634 y venta.php:307. Escenario mínimo que lo dispara: empresa Business con pixel_meta configurado; un asesor logueado pulsa 'Ver' en el editor → abre el slug público → Capa 0 lo excluye del Radar pero su navegador ejecuta los pixels y Meta/GA4/TikTok reciben PageView + ViewContent con el valor de la cotización. Cada previsualización interna infla audiencias de retargeting y métricas de campaña del cliente.
- **Fix:** Envolver scripts_base() y evento_view() en `if (!$es_usuario_interno)` (la variable ya existe en la línea 221) y hacer lo mismo en cotizacion_inmueble.php.

### 🟡 MEDIO — `core/Helpers.php:317`
**ip_real() confía en CF-Connecting-IP y X-Forwarded-For sin estar detrás de Cloudflare — la IP del tracking es spoofeable**

- **Evidencia:** Helpers.php:317: `foreach (['HTTP_CF_CONNECTING_IP', 'HTTP_X_FORWARDED_FOR', 'REMOTE_ADDR'] as $key)` — toma el primer header presente sin verificar que el request venga de Cloudflare (el sitio no usa CF según la propia documentación). filter_var acepta también IPs privadas (10.x, 192.168.x).
- **Impacto:** Cualquier cliente HTTP puede mandar `CF-Connecting-IP: <lo que sea>` directo al origen: (1) evade la deduplicación por IP y crea sesiones ilimitadas en la misma cot inflando visitas y la señal multi_persona/inminente (buckets calientes falsos bajo demanda), (2) evade el aprendizaje de IPs internas, (3) proxies corporativos que mandan XFF con IPs privadas colisionan entre clientes distintos. La señal 'IPs distintas' del Radar deja de ser confiable.
- **Escenario que lo dispara (verificado):** Confirmado en código real. ip_real() (core/Helpers.php:315-326) lee HTTP_CF_CONNECTING_IP y HTTP_X_FORWARDED_FOR ANTES de REMOTE_ADDR, sin verificar que el request provenga de un proxy confiable, y filter_var no usa FILTER_FLAG_NO_PRIV_RANGE (acepta 10.x/192.168.x). El sitio no está detrás de Cloudflare (CLAUDE.md: 'actualmente no usa CF'), por lo que el origen es alcanzable directo y cualquier cliente HTTP puede inyectar 'CF-Connecting-IP: <lo-que-sea>'. En public/cotizacion.php:151 este valor alimenta la dedup por IP (líneas 281-287 WHERE ip=?) y el INSERT de quote_sessions (293-296), y en modules/radar/Radar.php:977 el conteo de IPs distintas post-guest dispara el bucket multi_persona; la query de competencia usa COUNT(DISTINCT qs.ip) (1885). Escenario mínimo: un atacante anónimo que conoce un slug público envía N GET al slug, cada uno con CF-Connecting-IP distinto y un UA de navegador normal (cae a la rama cliente_real: Capa 0/1 requieren cookies, BOT_IP está vacío []). Cada request evade la dedup, crea una quote_session nueva, incrementa visitas y, al cruzar umbrales, empuja la cotización a multi_persona/buckets calientes disparando push falsos. Además rate_check/rate_hit usan ip_real(), así que el rate-limiting también se evade. Única imprecisión menor de la evidencia: toma el primer header que además valide como IP (no simplemente 'el primero presente'), irrelevante para el ataque.
- **Fix:** Ignorar CF/XFF salvo que REMOTE_ADDR esté en una lista blanca de proxies conocidos (rangos CF o el LB propio); si no, usar solo REMOTE_ADDR. Rechazar IPs privadas/reservadas con FILTER_FLAG_NO_PRIV_RANGE|NO_RES_RANGE en los headers forwarded.

### 🟡 MEDIO — `modules/dashboard/index.php:366`
**Ghost cleanup del dashboard decrementa visitas por resta y sin filtrar es_interno — descuenta dos veces sesiones ya excluidas**

- **Evidencia:** dashboard/index.php:364-367: `DELETE FROM quote_sessions WHERE id IN ($ph)` (sin `es_interno=0`) seguido de `UPDATE cotizaciones SET visitas = CASE WHEN visitas >= ? THEN visitas - ? ELSE 0 END`. Una sesión es_interno=1 ya fue excluida del contador por el recalc de layout.php:107-108 (`visitas = COUNT(... es_interno=0)`); si además se borra aquí, se resta otra vez. track.php:297-304 en cambio recalcula con COUNT (fuente de verdad).
- **Impacto:** El contador de visitas puede quedar por debajo del número real de visitas de clientes: sesión del asesor sin engagement → layout la marca interna y recalcula (correcto) → dashboard la borra y resta 1 adicional → una visita legítima de cliente desaparece del contador. El asesor ve '1 visita' donde hubo 2 y subestima el interés del cliente.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código real. dashboard/index.php:346-356 selecciona ghosts sin filtro es_interno y en 364-367 borra + resta por CASE, mientras layout.php:85-112 marca sesiones del asesor es_interno=1 y recalcula visitas=COUNT(es_interno=0) — esa sesión ya no cuenta en el contador. Escenario mínimo: (1) cotización con 1 visita de cliente; asesor abre el slug sin ser detectado y sin engagement/JS → sesión es_interno=0 scroll=0 visible=0, visitas=2. (2) Asesor abre cualquier página → layout cleanup la marca es_interno=1 y recalcula visitas=1 (correcto). (3) Asesor abre /dashboard → el ghost cleanup la selecciona (no filtra es_interno, cumple scroll=0/visible=0/>2min/sin quote_events con su IP), la borra y resta 1 → visitas=0 con 1 sesión real de cliente existente. track.php:297-304 usa el patrón seguro (recalc con COUNT es_interno=0), confirmando que la resta del dashboard es la anomalía. El contador no se autocorrige salvo que track.php encuentre ghosts futuros en esa cotización. No afecta estado/vista_at ni dispara 'sin abrir'; solo subestima el contador de visitas.
- **Fix:** Agregar `AND qs.es_interno = 0` al SELECT de ghosts y reemplazar la resta por el mismo recalc con subconsultas COUNT/MIN/MAX que usa track.php:297-304.

### 🟡 MEDIO — `api/track.php:157`
**track.php sobrescribe el visitor_id de la sesión cuando el fallback por IP fusiona a dos clientes distintos (CGNAT)**

- **Evidencia:** track.php:154-161: `UPDATE quote_sessions SET ... visitor_id=COALESCE(?, visitor_id), device_sig=COALESCE(?, device_sig) ...` — el parámetro va primero: si el evento trae vid, SIEMPRE pisa el vid existente. El lookup previo (líneas 124-132) matchea por IP cuando el vid no coincide: dos clientes distintos detrás del mismo CGNAT Telmex (200.68.x, caso documentado) en la misma cot dentro de 30 min comparten sesión, y el segundo reescribe la identidad del primero.
- **Impacto:** Dos decisores (esposo/esposa, socio) viendo la misma cotización desde la misma red: el Radar los colapsa en una sola persona y además la sesión cambia de dueño — la señal multi_persona (comprador acompañado = bucket caliente) se pierde justo en el escenario que más valor tiene, y el historial atribuye el engagement del primero al vid del segundo.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. Escenario mínimo: Cliente A (vid_A) abre la cotización desde el WiFi de su casa → cotizacion.php crea quote_session S con vid_A. Mientras A sigue activo (heartbeats/eventos refrescan updated_at → ventana de 30 min RODANTE), cliente B (vid_B, otro dispositivo, misma IP pública — cualquier hogar, no solo CGNAT) abre la misma cotización: (a) cotizacion.php:280-288 no encuentra sesión por vid_B, el fallback por IP matchea S → solo heartbeat, NO crea sesión ni cuenta la visita de B; (b) track.php:124-132 igual → sus eventos caen en S y el UPDATE de línea 156 `visitor_id=COALESCE(?, visitor_id)` (parámetro primero) pisa vid_A con vid_B — la sesión cambia de dueño y el engagement combinado (GREATEST de scroll/visible) queda atribuido al último vid. Consecuencia verificada en Radar.php: multi_persona (línea 970-981) exige guest_sessions >= 2 en modo 'medio' (default, tabla línea 106 + u() línea 246); con una sola fila fusionada guest_sessions=1 → el bucket NO dispara aunque e_uniq_v=2 (los eventos sí conservan cada vid) porque ese OR está detrás del gate en AND; el booster de prioridad (línea 785) usa solo vids de sesiones → también se pierde. Matices que bajan del impacto máximo: para dos visitas dentro de 30 min de creación el propio dedup del Radar (línea 506, dedupe_seconds=1800) colapsaría igual las sesiones por IP; en modo 'agresivo' (threshold=1) la señal sobrevive vía e_uniq_v; y con gap >30 min sin actividad de A no hay fusión. Pero en el caso rodante (A leyendo activamente cuando B entra después) el merge destruye una señal que el conteo del Radar sí habría preservado, y la visita de B nunca se cuenta. El comentario de cotizacion.php:267 ('IP como fallback cuando no hay cookie') confirma que la implementación diverge de la intención: el fallback aplica aunque el request traiga vid propio. Severidad medio se sostiene: el escenario (dos decisores en la misma red viendo la misma cotización en la misma sesión de lectura) es común y es justo el caso de valor del bucket multi_persona.
- **Fix:** Invertir el COALESCE a `visitor_id=COALESCE(visitor_id, ?)` (solo rellenar si estaba NULL); si el evento trae un vid distinto al de la sesión matcheada por IP, crear sesión nueva en vez de reusar.

### ⚪ BAJO — `public/cotizacion_inmueble.php:546`
**Inmuebles: czTrack('quote_accept'/'quote_reject') no están en tipos_validos de track.php — se descartan en silencio**

- **Evidencia:** cotizacion_inmueble.php:546 `if(window.czTrack) czTrack('quote_accept');` y :578 `czTrack('quote_reject')`. track.php:31-40 solo acepta 'accept_open','accept_confirm','reject_open','reject_confirm', y línea 40 `if (!in_array($tipo, $tipos_validos, true)) exit;`.
- **Impacto:** Los eventos JS de aceptación/rechazo del giro inmuebles nunca llegan a quote_events. El fallback de accept_confirm de track.php (crear venta si quote_action falló) no aplica para inmuebles, y las métricas de eventos por sesión pierden la señal de conversión final del template.
- **Fix:** Cambiar a czTrack('accept_confirm') y czTrack('reject_confirm') en cotizacion_inmueble.php (o agregar los alias a $tipos_validos).

### 🟡 MEDIO — `public/cotizacion.php:243`
**escudo_log quedó ciego para bots: capa_3_bot es inalcanzable (BOT_IP=[]) y es_bot(UA) sale sin registrar nada**

- **Evidencia:** cotizacion.php:243: `if (!es_bot($ua) && in_array($cot['estado'], ...))` — cuando es_bot() da true, se salta TODO el bloque incluyendo cualquier escudo_log_decision(). El único log de bots era capa_3_bot (línea 260), que depende de Radar::BOT_IP, hoy `const BOT_IP = []` (Radar.php:232). Resultado: ningún camino de bot deja rastro.
- **Impacto:** El monitor semanal documentado (query sobre decision='capa_3_bot' en escudo_log) devuelve 0 filas para siempre y los previews de WhatsApp/Telegram/facebookexternalhit no dejan evidencia. Si es_bot() produce un falso positivo (la clase de bug que ya costó el caso Rodano/iCloud, entonces detectado GRACIAS a escudo_log), esta vez no habrá datos para diagnosticarlo: se pierde la observabilidad que fue clave para cerrar ese incidente.
- **Escenario que lo dispara (verificado):** Verificado en código: (a) cotizacion.php:243 el guard `!es_bot($ua)` salta todo el bloque de tracking sin log alguno — grep confirma que escudo_log_decision solo existe dentro de ese bloque (líneas 248, 260, 297) más Capa 0 (237); (b) Radar.php:232 tiene `const BOT_IP = []`, lo que hace inalcanzable el único log de bots (capa_3_bot, línea 260) y deja muerto el monitor semanal documentado; (c) no duplica nada — es exactamente el pendiente #9 del audit del 24 mayo aún sin aplicar, y contradice la justificación del 29 mayo de que desactivar BOT_IP daba 'más observabilidad'. Valor de negocio real pero indirecto: protege la confiabilidad del estado 'sin abrir' ante falsos positivos de es_bot() (patrones amplios como 'bot','scanner','skype'), la misma clase de bug del caso Rodano que se diagnosticó gracias a escudo_log. Fix trivial (~3 líneas), riesgo casi nulo. No es alto/crítico porque no hay incidente activo y el tráfico humano sí queda registrado en quote_sessions.
- **Fix:** Antes del salto por es_bot($ua), llamar `escudo_log_decision('bot_ua', ...)`; y registrar también los estados no publicables si se quiere auditar cobertura completa (pendiente #9 del audit 24 mayo).

### ⚪ BAJO — `api/track.php:284`
**Ghost cleanup de track.php borra también sesiones es_interno=1 — destruye el rastro de auditoría del Escudo**

- **Evidencia:** track.php:283-295: `SELECT id FROM quote_sessions WHERE cotizacion_id = ? AND scroll_max = 0 AND visible_ms IS NOT NULL AND visible_ms < 200 AND created_at < DATE_SUB(NOW(), INTERVAL 2 MINUTE)` seguido de DELETE — sin `AND es_interno = 0`.
- **Impacto:** Las sesiones marcadas internas por el cleanup retroactivo son la evidencia de qué asesor contaminó qué cotización (base de las investigaciones tipo Calvario 18 / Manuel). Si el asesor no scrolleó, este DELETE las borra y la próxima auditoría de leaks pierde los datos; el recalc posterior no cambia contadores (ya excluían internas) pero la trazabilidad desaparece.
- **Escenario que lo dispara (verificado):** Verificado en código: track.php:284-289 no filtra es_interno=0 y el DELETE (línea 295) borra sesiones marcadas internas por la limpieza retroactiva de layout.php:84-93, que deliberadamente las MARCA en vez de borrarlas para preservar el rastro de auditoría — el DELETE contradice ese diseño. El fix de 1 línea es realista y no duplica nada existente. Sin embargo, el impacto es solo forense/operador: el recalc ya excluye internas (contadores, Radar y UI del vendedor no cambian), solo aplica a fugas de asesor con vistazo breve (<200ms sin scroll), y escudo_log mitiga parcialmente la trazabilidad. Valor concreto para las auditorías de leaks del superadmin (Calvario 18/Manuel), no valor diario para el vendedor PyME → prioridad baja. Bonus detectado al validar: el cleanup gemelo en dashboard/index.php:352-368 tampoco filtra es_interno y ahí sí decrementa visitas a ciegas — potencialmente más grave que lo reportado.
- **Fix:** Agregar `AND es_interno = 0` al SELECT de ghosts en track.php:284-289.


---

## Matemática del Score

### 🟠 ALTO — `core/ActividadScore.php:1606`
**snapshot_mensual falla SIEMPRE: ON DUPLICATE referencia columnas inexistentes en score_historial y el catch lo silencia**

- **Evidencia:** El ON DUPLICATE KEY UPDATE incluye `s_engagement=VALUES(s_engagement), eng_pen_sin_pago=VALUES(eng_pen_sin_pago), eng_pen_descuento=VALUES(eng_pen_descuento), eng_pen_enfriamiento=VALUES(eng_pen_enfriamiento)` (líneas 1606-1607), pero la tabla score_historial NO tiene esas columnas: verificado en migrations/add_score_historial.sql (líneas 18-30) y en el dump de producción migrations/cotizacl_cotizacloud.sql (líneas 3827-3852) — solo existen s_activacion, s_seguimiento, s_conversion. MySQL valida las columnas del UPDATE al preparar la sentencia (no solo al haber duplicado), así que TODA ejecución lanza 'Unknown column s_engagement' y el `catch (\Throwable $e) { /* tabla aún no migrada */ }` (línea 1629) lo traga sin log.
- **Impacto:** El snapshot mensual no se guarda desde que se agregaron esas líneas: el dump de producción solo tiene filas de 2026-03 en score_historial. Los reportes históricos/comparativas mensuales de vendedores quedan congelados o vacíos, y esos datos son irrecuperables (el score se recalcula rolling, la foto del mes se pierde para siempre). El dueño Business ve un histórico que no corre.
- **Escenario que lo dispara (verificado):** CONFIRMADO. Escenario mínimo: cualquier llamada a ActividadScore::recalcular_empresa() (línea 1151) invoca snapshot_mensual(), cuyo INSERT ... ON DUPLICATE KEY UPDATE referencia s_engagement, eng_pen_sin_pago, eng_pen_descuento y eng_pen_enfriamiento (líneas 1606-1607) — columnas que NO existen en score_historial (verificado en migrations/add_score_historial.sql y en el dump de producción líneas 3827-3852; esas columnas solo se agregaron a usuario_score vía add_s_engagement.sql y add_eng_pen_cols.sql). MySQL resuelve todas las columnas de la sentencia al ejecutarla, con o sin duplicado, así que lanza error 1054 'Unknown column s_engagement' en el primer INSERT de cada vendedor; PDO está en ERRMODE_EXCEPTION (DB.php:26) y el catch(\Throwable) de la línea 1629 lo traga sin log. Evidencia en producción: score_historial solo tiene 2 filas, ambas de 2026-03 — no se ha escrito nada desde entonces y esas fotos mensuales son irrecuperables (el score es rolling). Ajusto severidad de crítico a alto porque grep del repo muestra que NINGÚN código lee score_historial hoy (solo el writer y las migraciones): no hay reporte visible roto para el usuario, pero la pérdida silenciosa e irreversible de datos mensuales es real y bloqueará cualquier reporte histórico futuro. Fix: quitar las 4 columnas fantasma del ON DUPLICATE (o agregarlas a la tabla + al INSERT) y loguear el error en el catch.
- **Fix:** Quitar las 4 asignaciones de columnas inexistentes del ON DUPLICATE (o correr migración que agregue s_engagement/eng_pen_* a score_historial). Además loggear el Throwable con error_log en vez de tragarlo.

### 🟠 ALTO — `core/ActividadScore.php:432`
**pen_sin_pago: numerador sin ventana vs denominador de 15 días — una venta vieja sin cobrar deja Engagement en 0 para siempre**

- **Evidencia:** ventas_sin_pago (líneas 432-438) cuenta `pagado = 0 AND estado NOT IN ('cancelada','entregada') AND created_at < DATE_SUB(NOW(), INTERVAL 5 DAY)` SIN límite inferior de fecha — es all-time. El denominador ventas_totales (líneas 447-453) sí filtra `created_at >= DATE_SUB(NOW(), INTERVAL ? DAY)` (15d) y exige pagado>0. La penalización (línea 461) es `($ventas_sin_pago / $ventas_totales_safe) * (1.0 / $close_rate_safe)` con clamp a 1.0.
- **Impacto:** Escenario: un vendedor tiene 1 venta de hace 4 meses con pagado=0 (nunca se marcó entregada/cancelada). Este período vende y cobra 3 ventas. Con close_rate 0.15: pen = min((1/3)×6.67, 1.0) = 1.0 → s_engagement = 0 → pierde los 17 puntos completos del score, mes tras mes, sin importar cuánto venda y cobre ahora. Es exactamente el patrón reportado como 'Kevin: score baja cuando vende'. Vender más no lo sube; solo editar la venta vieja lo arregla.
- **Escenario que lo dispara (verificado):** Confirmado leyendo core/ActividadScore.php. Escenario mínimo: un vendedor con UNA venta antigua (fuera de la ventana de 15 días, p.ej. de hace 4 meses) que quedó con pagado=0, estado distinto de 'cancelada'/'entregada' y total>0. La query del numerador (líneas 432-439) no tiene cota inferior de fecha (solo created_at < NOW-5d), así que esa venta cuenta all-time. El denominador (líneas 447-453) sí filtra created_at >= NOW-15d y pagado>0. Con 3 ventas cobradas este período y close_rate 0.15: pen = min((1/3)×(1/0.15), 1.0) = min(2.22, 1.0) = 1.0 → s_engagement = max(0, 1-1) = 0 (líneas 506-507), perdiendo los 17 puntos completos ($w_eng=0.17, línea 797) de forma permanente: vender y cobrar más no lo repara (necesitaría >6.67 ventas cobradas cada 15 días solo para que la pen baje de 1.0, y nunca llega a 0 mientras exista la venta vieja). Único matiz: el EMA (línea 839) suaviza la caída, pero converge a 0 y se queda ahí. El impacto se limita a la dimensión Engagement (17% del score), no al score completo, pero es un castigo perpetuo e irrecuperable por desempeño actual — exactamente el patrón 'Kevin: score baja aunque venda'.
- **Fix:** Acotar el numerador a la misma ventana del período: agregar `AND created_at >= DATE_SUB(NOW(), INTERVAL ? DAY)` (con $periodo) a la query de ventas_sin_pago, o al menos un tope (ej. 60 días) coherente con el denominador.

### 🟠 ALTO — `core/ActividadScore.php:928`
**w_momentum sin tope: en empresas con close_rate alto, el momentum domina el score y un vendedor perfecto queda topado en ~60-70**

- **Evidencia:** `$w_momentum = (1.0 - $w_percentil) * $close_rate_safe;` — close_rate_safe viene del benchmark de ventana (piso 0.03 pero SIN techo, línea 1571). Una empresa chica con 6 vistas y 4 cierres en 15 días da close_rate 0.67 → w_momentum = 0.67 y w_proporcional = 0.33. En estado estable momentum_score = 0.50 exacto (línea 862: sigmoid de log(1.0)=0).
- **Impacto:** Vendedor único con las 5 dimensiones en 1.0 y desempeño estable: final = 0.33×1.0 + 0.67×0.50 = 0.665 → score 66-67 'Activo' bajo, imposible llegar a 'Top' (86) por matemática pura, mientras un vendedor idéntico en una empresa que cierra 10% sí llega a ~85. El score deja de medir al vendedor y pasa a medir el close_rate de su empresa — erosiona la confianza en el leaderboard entre empresas del mismo dueño (OnTime 3 sucursales).
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código. Escenario mínimo: empresa con 1-2 vendedores (w_percentil=0, línea 925-927) y ≥5 cotizaciones vistas en la ventana de 15 días con close_rate alto — ej. 6 vistas / 4 cierres pagados → close_rate=0.667 (línea 1492, piso 0.03 en 1571 pero SIN techo). Entonces w_momentum=(1-0)×0.667=0.667 (línea 928) y w_proporcional=0.333. En estado estable el EMA converge al proporcional actual → ratio=1.0 → momentum_score=sigmoid(3·log(1))=0.50 exacto (líneas 846-862). Vendedor con las 5 dimensiones en 1.0: final=1.0×0.333+0.50×0.667=0.667 → score 67 (línea 931-935). El mismo vendedor en empresa con close_rate 10% da 0.95 → 95. Caso extremo: close_rate=1.0 (5/5) → w_proporcional=0.0 y el score de un vendedor perfecto estable es 50. Matiz que no invalida: bonus_ticket (+15 máx) y bonus_cierre (+8 máx) se suman después (líneas 943-990) y en teoría permiten superar el techo, pero requieren condiciones incompatibles con desempeño estable (cierre ≥2.5× el histórico propio; tickets 1.5×+ sobre un promedio que se auto-infla). Con datos reales del proyecto (Kevin, 47% cierre) el techo base es ~76: 'Top' (86) inalcanzable por matemática. El score pasa a medir el close_rate de la empresa, no al vendedor.
- **Fix:** Capar w_momentum, ej. `$w_momentum = (1.0 - $w_percentil) * min($close_rate_safe, 0.25);` para que proporcional nunca pese menos de ~50%.

### 🟠 ALTO — `core/ActividadScore.php:429`
**tasa_cierre mezcla ventanas: cierres por accion_at vs vistas por created_at — puede superar 100% e infla sigmoid, piso y bonus**

- **Evidencia:** `$tasa_cierre = $cierres_total / $cot_vistas_safe;` donde cierres_total (líneas 240-246) filtra `accion_at >= DATE_SUB(NOW(), INTERVAL ? DAY)` (sin filtro de created_at) y cot_vistas (líneas 205-210) filtra `created_at >= DATE_SUB(NOW(), INTERVAL ? DAY)`. Numerador y denominador cuentan poblaciones distintas.
- **Impacto:** Escenario real con TTC de 14 días: el vendedor cierra 3 cotizaciones creadas hace 2 meses (accion_at esta semana, pagadas) y solo tiene 1 cotización nueva vista → tasa_cierre = 3/1 = 300%. La sigmoid satura, perf_ratio = 1.0 (piso completo, línea 714), y con ≥4 cierres el bonus_cierre de +8 se dispara por artefacto de ventana, no por mérito. Al período siguiente ocurre lo inverso (las cotizaciones nuevas aún no cierran) y el score se desploma — oscilación que el vendedor percibe como 'vendo y baja'.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código: línea 429 divide cierres_total (filtrado por accion_at en ventana de 15d, SIN filtro de created_at — líneas 240-246) entre cot_vistas (filtrado por created_at en la misma ventana — líneas 205-210). Poblaciones disjuntas sin clamp a 1.0. Escenario mínimo: vendedor con TTC largo acepta (con pagado>0) 4 cotizaciones creadas hace 2 meses dentro de la ventana actual y solo tiene 1 cotización nueva vista → tasa_cierre = 4/1 = 400%. Efectos verificados en código: sigmoid satura (línea 703), perf_ratio = 1.0 activa el piso completo conv_floor (líneas 714-716), y bonus_cierre +8 se dispara con ratio 400%/hist≈10% = 40× (líneas 977-981, requiere cierres_total>=4). El período siguiente esos cierres salen de la ventana de accion_at mientras las cotizaciones nuevas entran al denominador sin haber cerrado aún → tasa se desploma, pen_volumen_sin_cierre aplica y se pierden piso y bonus → oscilación 'vendo y baja' consistente con el reporte de Kevin documentado en CLAUDE.md. Matiz menor: el bonus requiere >=4 cierres, no 3 como dice el ejemplo del hallazgo, pero el mecanismo es idéntico.
- **Fix:** Usar la misma población: contar cierres de cotizaciones con `created_at >= ventana` (cohorte), o denominar sobre cotizaciones vistas en cualquier fecha cuyos cierres caen en la ventana.

### 🟡 MEDIO — `core/ActividadScore.php:1515`
**close_rate_hist sesgado a la baja por el corte accion_at vs created_at — infla sistemáticamente bonus_cierre**

- **Evidencia:** El denominador histórico (líneas 1498-1504) usa `created_at < DATE_SUB(NOW(), INTERVAL ? DAY)` pero el numerador (líneas 1505-1512) usa `accion_at < DATE_SUB(NOW(), INTERVAL ? DAY)`. Toda cotización creada antes del corte pero aceptada DENTRO de la ventana actual cuenta como 'vista histórica sin cierre histórico': su cierre sale del numerador hist (cae en la ventana actual) pero la cotización queda en el denominador hist.
- **Impacto:** Con periodo=15d y TTC≈14d, una fracción grande de los cierres de cotizaciones 'históricas' cae siempre en la ventana actual → close_rate_hist queda estructuralmente por debajo del real. Como bonus_cierre usa `$ratio_cierre = $tasa_cierre / $bench['close_rate_hist']` con tiers 2.5×→+4 y 4×→+8 (líneas 977-981), y tasa_cierre además está inflada por el hallazgo anterior, los +4/+8 puntos se regalan por sesgo de ventana. Los casos documentados (Kevin 8.6%→47% = 5.5×) pueden estar parcialmente fabricados por este sesgo.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo el código. Escenario mínimo: cotización creada día −20 y aceptada con pago día −5 (periodo=15). (1) En close_rate_hist: entra al denominador hist (línea 1502: created_at < cutoff, y el OR de estado aceptada en 1501 la incluye) pero su cierre queda fuera del numerador hist (línea 1509: accion_at < cutoff, y accion_at=−5d ≥ cutoff). Como accion_at ≥ created_at siempre, el numerador es subconjunto del denominador → sesgo estrictamente a la baja. (2) La misma cotización infla la tasa_cierre del vendedor: $cierres_total (línea 243) filtra accion_at >= cutoff sin filtro de created_at, pero $cot_vistas (línea 207) filtra created_at >= cutoff → cuenta en numerador sin contar en denominador (línea 429). (3) ratio_cierre = tasa_cierre / close_rate_hist (línea 978) queda doblemente inflado y otorga +4/+8 (líneas 979-981); close_rate_hist deflactado también relaja pen_volumen_sin_cierre (línea 679) y el sigmoid de Conversión. MATIZ: la magnitud del sesgo hist decae con la antigüedad de la empresa (solo excluye cierres de los últimos 15 días contra un denominador que acumula toda la historia): ~4-10% en empresas maduras, ~15-25% en jóvenes. El bonus está topado en +8 y en el caso Kevin (5.5×) el tier de 4× se mantendría aun corrigiendo el sesgo — distorsiona pero no fabrica por completo los casos documentados. Por eso ajusto de alto a medio.
- **Fix:** Calcular el histórico por cohorte: cotizaciones con created_at < corte y su resultado final sin importar cuándo cerró (`estado IN ('aceptada',...)` sin filtro de accion_at en el numerador).

### 🟡 MEDIO — `core/ActividadScore.php:105`
**GRACIA_DIAS=7 'temporal para testing' quedó en producción — vendedores nuevos evaluados con ventana incompleta**

- **Evidencia:** `private const GRACIA_DIAS = 7; // temporal para testing — volver a 15`. El propio comentario y el docblock (línea 111-113: 'PERÍODO DE GRACIA — 15 días (= PERIODO). Se necesita un período completo de datos para un score justo') dicen que debe ser 15. PERIODO = 15 (línea 25).
- **Impacto:** Entre el día 7 y el 14, un vendedor nuevo se evalúa con una ventana rolling de 15 días de la cual solo vivió la mitad: cot_asignadas/vistas/ventas bajas contra benchmarks de empresa completos → pen_bajo_benchmark y vol_trend lo castigan, y aparece públicamente como 'Bajo' en el leaderboard. Es exactamente el pendiente documentado ('Vendedor nuevo 7-9 días penalizado injustamente') que el fix de 15 días resolvía y quedó desactivado.
- **Escenario que lo dispara (verificado):** CONFIRMADO leyendo core/ActividadScore.php. Línea 105 dice literalmente `GRACIA_DIAS = 7; // temporal para testing — volver a 15`, contradiciendo su propio docblock (líneas 111-113: gracia = 15 días = PERIODO) y el diseño documentado ('Gracia 15 días para vendedores nuevos' en v5). Escenario mínimo: vendedor crea su primera cotización hace 8 días en una empresa con ventas en el período anterior → dias_en_plataforma=8 ≥ 7 → el gate de la línea 136 NO lo protege → se calcula score completo con ventana rolling de 15 días de la que solo vivió 8. Verificado con grep que dias_en_plataforma no se usa en ningún otro punto (no hay prorrateo): eng_pen_bajo_benchmark (líneas 485-501) compara sus ventas parciales contra bench_ventas = promedio por vendedor de la empresa del período anterior completo, y vol_trend (línea 688) = min(ventas/bench_ventas,1) entra a Conversión. Resultado: score deprimido y etiqueta pública baja en el leaderboard durante los días 7-14. Ajusto severidad de alto a medio porque la ventana de exposición es acotada (7 días por vendedor nuevo), se autocorrige al día 15, y no hay corrupción de datos ni impacto monetario — pero el fix es trivial (volver la constante a 15) y debe aplicarse.
- **Fix:** Cambiar `GRACIA_DIAS = 7` a `= 15` (o `= self::PERIODO`) como indica el propio comentario.

### 🟡 MEDIO — `core/ActividadScore.php:901`
**Percentil compara escalas distintas: mi proporcional crudo (sin bonus/momentum) contra el score final de los compañeros (con bonus hasta +23)**

- **Evidencia:** Para el usuario actual se usa `$scores_equipo[] = ['s' => $proporcional, ...]` (línea 897, valor 0-1 ANTES de momentum, percentil, bonus_ticket y bonus_cierre) mientras que para los demás `$s_other = (float)($t['sc'] ?? 0) / 100.0;` (línea 901) — el score FINAL persistido, que ya incluye w_momentum, w_percentil previo y hasta +15 de bonus_ticket y +8 de bonus_cierre. Además `COALESCE(us.score, 0)` (línea 871) hace que un compañero aún no calculado cuente como 0.
- **Impacto:** Dos vendedores con proporcional idéntico 0.60: el compañero fue calculado antes y tiene bonus_ticket +15 → su score guardado 75 vs mi 0.60 → yo quedo abajo en percentil aunque seamos iguales; con team de 5 eso mueve hasta 25% del peso final (w_percentil cap 0.25). El ranking del leaderboard depende del ORDEN en que se recalculan los usuarios, no solo de su desempeño.
- **Escenario que lo dispara (verificado):** Confirmado leyendo core/ActividadScore.php. Escenario mínimo: empresa con 3+ vendedores activos (team_size>=3 para que w_percentil>0). Vendedor A y B tienen proporcional idéntico 0.60. B fue recalculado antes y su score persistido incluye bonus_ticket +15 (línea 967) y/o bonus_cierre +8 (línea 982) → us.score=75. Al recalcular A, la línea 897 mete su proporcional crudo (0.60) al array mientras la línea 901 mete 0.75 para B (score final /100). El usort (línea 905) coloca a A debajo de B aunque su desempeño base sea igual → percentil de A baja → su score final baja hasta ~w_percentil×Δpos (2-8 puntos según tamaño de equipo). Adicionalmente COALESCE(us.score,0) en línea 871 hace que un compañero nunca calculado cuente como 0 e infle el percentil propio. El comentario de línea 899 dice "usar tasa_gestion (proporcional guardado)" pero el query selecciona us.score — la columna tasa_gestion (que guarda el proporcional, línea 1053) existe y sería la comparación de escalas homogéneas. Resultado: el ranking depende del orden de recálculo, no solo del desempeño. Severidad: el impacto está acotado (w_percentil cap 0.25 solo con n>=10; con equipos de 3-5 es 0.05-0.13, desplazamiento típico 2-8 puntos de 100), por eso se mantiene en medio y no alto.
- **Fix:** Comparar magnitudes iguales: usar `us.tasa_gestion` (proporcional persistido) para los compañeros en vez de `us.score`, o persistir primero y comparar todos por score final.

### 🟡 MEDIO — `core/ActividadScore.php:994`
**Columna 'bonuses' persiste un bonus fantasma que nunca se aplica al score — el debug panel muestra un premio inexistente**

- **Evidencia:** `$total_bonus = ($cierre_quality > 0 ? $cierre_quality * $close_rate_safe : 0);` se calcula, se persiste como `bonuses` (línea 1052) y se retorna, pero NINGUNA línea lo suma al score: el flujo es proporcional → final (931-933) → +bonus_ticket (967) → +bonus_cierre (982). El dashboard lo muestra como real: modules/dashboard/index.php:1119 `<span class="dbg-val"><?= round(($es['bonuses'] ?? 0) * 100, 1) ?>%</span>` y executive.php:429 lo selecciona.
- **Impacto:** El superadmin ve 'Bonuses +7.5%' en el panel debug de un vendedor cuyo score jamás recibió ese bonus. Al auditar por qué un score es 59 y no 66, los números del panel no cuadran con el score — mina la confianza en el termómetro que es justo lo que se está calibrando con esos paneles.
- **Escenario que lo dispara (verificado):** CONFIRMADO. Línea 994 de core/ActividadScore.php calcula $total_bonus = cierre_quality × close_rate_safe, se persiste como columna 'bonuses' (línea 1052) y se retorna (1105), pero grep exhaustivo confirma que NINGUNA línea lo suma al score: el flujo es proporcional+piso (802-810) → final ponderado (931-933) → +bonus_ticket (967) → +bonus_cierre (982). cierre_quality solo participa en el score como sub-componente ponderado de s_conversion (línea 705), que es una fórmula distinta. El dashboard (modules/dashboard/index.php:1119) muestra 'Bonuses X%' en el panel debug junto a Penalizaciones (que sí son reales), y executive.php:429 lo selecciona. Escenario mínimo: cualquier vendedor con ≥1 cierre con bucket en el período (cierre_quality>0) y close_rate>0 — el superadmin ve un bonus (ej. 7.5%) que el score jamás recibió, y al auditar la composición del score los números no cuadran. Es el residuo del 'bonus auto-ajustable' de v5.1 que se removió del flujo pero cuyo cálculo/columna/display quedaron huérfanos.
- **Fix:** Eliminar $total_bonus y persistir 0 (o persistir bonus_ticket+bonus_cierre reales en 'bonuses'), y ajustar el panel para mostrar solo bonus aplicados.

### 🟡 MEDIO — `core/ActividadScore.php:401`
**no_abiertas_5d ignora el filtro de importación masiva ($no_import) — una importación sin visitas mata Activación indefinidamente**

- **Evidencia:** La query de no_abiertas_5d (líneas 401-406) usa `WHERE $cw $no_susp AND estado='enviada' AND visitas=0 AND created_at < DATE_SUB(NOW(), INTERVAL 5 DAY)` — no incluye `$no_import`, mientras cot_asignadas, cot_vistas y dormidas sí lo aplican (líneas 201, 206, 217). Con no_abiertas_5d>0, `$s_activacion_op = 0.0 - $pen_dormidas` (línea 419).
- **Impacto:** Una empresa importa 30 cotizaciones históricas en un día (detectadas y excluidas del resto del score por el filtro >20/día, línea 183). Si alguna queda 'enviada' con visitas=0 —lo normal en importaciones—, a los 5 días la Activación operativa del vendedor cae a 0 y ahí se queda para siempre (la query tampoco tiene ventana), aunque todas sus cotizaciones reales se abran. Trabajo real invisible: esforzarse no sube el score.
- **Escenario que lo dispara (verificado):** Confirmado en core/ActividadScore.php:401-406: la query de no_abiertas_5d no incluye $no_import (a diferencia de cot_asignadas L200-201, cot_vistas L205-208 y dormidas L216-235 que sí lo aplican), no tiene ventana de tiempo inferior y ni siquiera exige total>0. Escenario mínimo que lo dispara: una empresa importa 21+ cotizaciones históricas en un mismo día asignadas a un vendedor, y al menos 1 queda con estado='enviada' y visitas=0 (lo normal en importaciones). El filtro de importación (L178-189) las excluye de asignadas/vistas/dormidas, pero a los 5 días la query L401 las cuenta → pen_no_abiertas=1.0 (L410) → s_activacion_op = 0 - pen_dormidas (L418-419, puede ir negativo, sin clamp a 0 según L423). Como la query no tiene ventana, la penalización persiste indefinidamente aunque todas las cotizaciones reales del vendedor se abran — solo se libera suspendiendo o cambiando de estado manualmente las importadas. Agravante: incluso agregando $no_import no bastaría a largo plazo, porque import_dates (L182) solo detecta importaciones dentro de la ventana $periodo, mientras que la query de no_abiertas_5d mira todo el histórico. Severidad medio es correcta: el daño se acota a la dimensión Activación (13% del score, mitad operativa + arrastre negativo), no tumba el score completo pero lo degrada permanentemente.
- **Fix:** Agregar `$no_import` a la query de no_abiertas_5d (misma exclusión que las demás métricas de Activación).

### 🟡 MEDIO — `core/ActividadScore.php:1474`
**Cache de _benchmarks ignora el parámetro $periodo — vendedores de la misma empresa pueden evaluarse con períodos/benchmarks mezclados**

- **Evidencia:** `if (isset(self::$_bench[$empresa_id])) return self::$_bench[$empresa_id];` — el cache está keyed solo por empresa_id, pero calcular() lo invoca con períodos distintos: primero `self::_benchmarks($empresa_id, self::PERIODO)` (línea 161) y si ttc>20 recalcula con período extendido (líneas 166-169) dejando cacheada la versión extendida. El siguiente usuario del loop de recalcular_empresa pide PERIODO=15 pero recibe el bench de 45/60 días; si el ttc de esa ventana larga sale ≤20, su `$periodo` local queda en 15 mientras sus benchmarks (close_rate, apertura, bench de radar) son de 45-60 días.
- **Impacto:** En empresas de ciclo largo, dos vendedores del mismo leaderboard se comparan contra benchmarks de ventanas distintas y hasta con períodos de evaluación distintos según el orden del loop — diferencias de score entre compañeros que no reflejan desempeño sino orden de cálculo.
- **Escenario que lo dispara (verificado):** Confirmado leyendo el código: ActividadScore.php:1474 cachea benchmarks keyed solo por empresa_id ignorando $periodo, y calcular() (161-170) deja cacheada la versión extendida cuando ttc>20. Escenario mínimo: empresa con 2 vendedores donde la ventana de 15d tiene ttc>20 (ej. un cierre de 30d). Vendedor A extiende a 45d y cachea ese bench; vendedor B pide bench de 15d en línea 161 pero recibe el de 45d por cache hit. Si el ttc del bench de 45d sale ≤20 (cierres viejos rápidos bajan el promedio), B se evalúa con $periodo=15 en sus señales crudas pero contra close_rate/apertura/radar_weekly de 45d, mientras A se evaluó con periodo=45. El unset de recalcular_empresa (línea 1133) es previo al loop y no evita la contaminación entre iteraciones. Resultado dependiente del orden del loop.
- **Fix:** Incluir el período en la key del cache: `self::$_bench[$empresa_id.'-'.$periodo]`, y decidir el período extendido UNA vez por empresa en recalcular_empresa().

### ⚪ BAJO — `core/ActividadScore.php:991`
**Columna 'penalizaciones' exagera el impacto real: cuenta pen_no_abiertas=1.0 y pen_dormidas a peso completo cuando en el score entran al 50%**

- **Evidencia:** `$total_pen = ($pen_no_abiertas + $pen_dormidas) * $w_act + ...` — pero en el score real la parte operativa de Activación pesa solo 50% de la dimensión (línea 425: `$s_activacion = ($s_activacion_op * 0.5) + ($tips_score * 0.5)`), y pen_no_abiertas=1.0 es un flag, no la pérdida real (la pérdida real es tasa_apertura×0.5×w_act, no 1.0×w_act). El comentario promete 'impacto real en score' (línea 990) y el panel lo muestra como '-X%' (dashboard/index.php:1118).
- **Impacto:** Vendedor con 1 no-abierta-5d y tasa_apertura 0.8: el panel muestra penalización -13% (1.0×0.13) cuando la pérdida real en el score es ~5.2 puntos (0.8×0.5×0.13). El asesor/dueño hace la cuenta y no le da — el 'desglose de penalizaciones' pierde credibilidad.
- **Escenario que lo dispara (verificado):** Confirmado. Escenario mínimo: vendedor con 1 cotización no-abierta-5d y tasa_apertura 0.8 — línea 991 computa total_pen = 1.0 × 0.13 = 0.13 (panel muestra "-13%"), pero la pérdida real en el score es tasa_apertura × 0.5 × w_act = 0.8 × 0.5 × 0.13 ≈ 5.2 puntos, porque (a) pen_no_abiertas=1.0 es un flag (línea 410) cuyo efecto real es solo anular la operativa, y (b) la operativa pesa 50% de Activación (línea 425: s_activacion = op×0.5 + tips×0.5), factor que la línea 991 omite. Lo mismo aplica a pen_dormidas (exagerada 2×). El comentario "impacto real en score" (línea 990) es falso. MATIZ que reduce severidad: la fila "Penalizaciones" solo se muestra en el panel debug del superadmin (dashboard/index.php:1093 gate Auth::es_superadmin(); la 1118 está dentro), y $ts_pen del termómetro del asesor (línea 919) se asigna pero nunca se renderiza — el asesor/dueño NO ve este número, solo el superadmin. No afecta el cálculo del score, solo el desglose informativo.
- **Fix:** Calcular total_pen con el impacto efectivo: `(($no_abiertas_5d>0 ? $tasa_apertura : 0) + $pen_dormidas) * 0.5 * $w_act + ...`.

### ⚪ BAJO — `core/ActividadScore.php:1045`
**Columna radar_views persiste fb_total pero calcular() retorna radar_sessions bajo la misma llave — el panel ejecutivo muestra un dato distinto al del cálculo en vivo**

- **Evidencia:** En el INSERT, la columna radar_views recibe `$fb_total` (línea 1045: `$fb_total, round($cots_calientes, 1), ...` mapeando a `radar_views, radar_benchmark, ...` de línea 1005), pero el array retornado define `'radar_views' => $radar_sessions ?? 0` (línea 1124) — número de sesiones de Radar, semánticamente distinto al número de feedbacks. modules/superadmin/executive.php:428 selecciona `us.radar_views` de BD.
- **Impacto:** Mismo vendedor, mismo momento: el panel que lee de BD (executive) muestra radar_views=3 (feedbacks) y una vista que use el retorno en vivo de calcular() muestra radar_views=27 (sesiones). Cualquier auditoría cruzada de los paneles debug encuentra números incompatibles con el mismo nombre.
- **Fix:** Persistir fb_total en su propia columna (o reusar una legacy con comentario, como se hizo con transiciones_up) y dejar radar_views = $radar_sessions en ambos lados.

### ⚪ BAJO — `core/ActividadScore.php:1119`
**pen_seguimiento retornada para debug pero nunca definida — siempre 0 aunque pen_buckets sí penalice Seguimiento**

- **Evidencia:** `'pen_seguimiento' => round($pen_seguimiento ?? 0, 3),` — la variable $pen_seguimiento no existe en ningún punto de calcular() (el `?? 0` sobre variable indefinida siempre da 0). La penalización real de la dimensión es `$pen_buckets = $buckets_estancados / $asignadas_validas` (línea 590), que se resta en línea 600 pero no se expone ni persiste.
- **Impacto:** En el desglose de penalizaciones del debug panel, un vendedor con 5 buckets estancados de 10 asignadas pierde 0.5 de Seguimiento (12.5 puntos ponderados) pero pen_seguimiento muestra 0.000 — el dato que explicaría la caída de su dimensión más pesada después de Conversión es invisible.
- **Fix:** Retornar/persistir `'pen_seguimiento' => round($pen_buckets, 3)`.


---

## Core / Infra

### 🟠 ALTO — `core/DB.php:123` _(verificado inline)_
- **Evidencia:** siguiente_folio hace INSERT ... ON DUPLICATE KEY UPDATE ultimo = ultimo + 1 (atómico), pero en la rama UPDATE LAST_INSERT_ID() da 0 y cae a un SELECT separado (128-133) para leer 'ultimo' — ese SELECT NO está en la operación atómica.
- **Impacto:** Bajo concurrencia: A actualiza 5→6, B actualiza 6→7, ambas hacen el SELECT y leen 7 → dos ventas con el MISMO folio y el 6 saltado. Folios duplicados = caos contable para el cliente.
- **Fix:** Usar ON DUPLICATE KEY UPDATE ultimo = LAST_INSERT_ID(ultimo + 1) y leer SOLO LAST_INSERT_ID(); eliminar la rama del SELECT.

### 🟠 ALTO — `core/MercadoPago.php:135` _(verificado inline)_
- **Evidencia:** El Idempotency-Key incluye bin2hex(random_bytes(4)): cz_renew_{empresa}_{Ymd}_{random}. La clave debe ser ESTABLE entre reintentos, no aleatoria.
- **Impacto:** Si el POST de cobro hace timeout y se reintenta, la clave nueva hace que MP lo trate como cargo NUEVO → doble cargo real al cliente.
- **Fix:** Clave determinística: cz_renew_{empresa}_{YYYYMM} sin random.

### 🟠 ALTO — `core/Helpers.php:384` _(verificado inline)_
- **Evidencia:** trial_info() ejecuta DDL (ALTER TABLE empresas ADD/MODIFY COLUMN) en 384/388/400 dentro del request.
- **Impacto:** En cada carga que llame trial_info(), si la columna existe el ALTER falla y se descarta, gastando el hot path en cada request. En tabla grande sin la columna, bloquea con lock de esquema.
- **Fix:** Mover los ALTER a migración one-shot y quitarlos de trial_info().

### 🟡 MEDIO — `cron/procesar_suscripciones.php:184` _(verificado inline)_
- **Evidencia:** El degradado busca plan!=free y plan_vence<hoy sin grace; solo salta si tiene mp_customer_id. Las licencias MANUALES (gift/transferencia por toggle_plan) no tienen fila en suscripciones → se degradan. El UPDATE pone activa=1.
- **Impacto:** Una licencia de cortesía o pagada por transferencia se degrada a Free al vencer sin aviso; y si el superadmin suspendió la empresa (activa=0), el cron la reactiva (activa=1). El control manual se pisa.
- **Fix:** No degradar (o avisar antes) licencias manuales vigentes sin mp_customer_id; no tocar 'activa' en el degradado.

### 🟡 MEDIO — `core/MercadoPago.php:458` _(verificado inline)_
- **Evidencia:** El webhook inserta el status crudo de MP en pagos_suscripcion.estado ENUM('approved','pending','rejected','refunded').
- **Impacto:** MP manda estados fuera del set (in_process, authorized, charged_back, cancelled): el INSERT revienta (estricto) y el webhook pierde el evento sin reintento, o lo trunca a '' (no estricto).
- **Fix:** Mapear estados MP al ENUM o ampliarlo, y validar antes del INSERT.

### 🟡 MEDIO — `core/Mailer.php:50` _(verificado inline)_
- **Evidencia:** catch (MailException) { if (DEBUG) error_log(...); return false; }. En producción DEBUG=false → el error NUNCA se loguea.
- **Impacto:** Correo falla (SMTP caído, credenciales malas) → silencio absoluto: resets de contraseña, avisos al superadmin, notificaciones de abono — nadie se entera de que dejaron de salir.
- **Fix:** Loguear SIEMPRE el error de correo, no solo con DEBUG.

### ⚪ BAJO — `core/PushNotification.php:118`
**badge_count se incrementa aunque el push falle: badge del iPhone se infla permanentemente**

- **Evidencia:** En enviar_a_empresa (igual en enviar_a_usuario:77 y enviar_a_superadmin:347): `DB::execute("UPDATE dispositivos_push SET badge_count = badge_count + 1 WHERE id = ?"...)` se ejecuta ANTES del try/catch del envío APNs/WebPush. Si el envío falla ($ok=false), el contador no se revierte.
- **Impacto:** Dispositivo con fallos intermitentes de APNs acumula badge_count fantasma: el siguiente push exitoso muestra un badge de p.ej. '7' cuando solo hay 1 notificación real — el asesor cree que se perdió actividad del Radar que nunca existió, erosionando la confianza en las notificaciones.
- **Fix:** Incrementar badge_count solo después de envío exitoso, o decrementarlo en el catch cuando $ok sea false.

### ⚪ BAJO — `core/Helpers.php:198`
**input_int/input_float nunca devuelven el default por precedencia de operadores**

- **Evidencia:** `return (int) ($input = input($key)) !== '' ? (int)$input : $default;` — se evalúa como `((int)$input !== '') ? ...` y un int nunca es idéntico a '' → la condición siempre es true y SIEMPRE retorna (int)$input (0 si el parámetro no vino), jamás $default. Igual en input_float (línea 203). Hoy sin llamadores (verificado con grep), pero es una trampa armada.
- **Impacto:** El primer dev que use `input_int('pagina', 1)` obtendrá 0 en vez de 1 con el parámetro ausente — paginación/offsets rotos de forma sutil y difícil de rastrear.
- **Fix:** `function input_int(string $key, int $default = 0): int { $v = input($key); return $v !== '' ? (int)$v : $default; }` (idem float).


---

## Producto — mejoras (lente CEO)

### 🟠 ALTO — `modules/radar/index.php:228`
**El Radar detecta intención pero no la convierte en acción: teléfono del cliente cargado y nunca usado, mensajes del playbook sin botón WhatsApp ni copiar**

- **Evidencia:** La query carga el teléfono ('cl.nombre AS cnombre, cl.telefono AS ctel' línea 82) y lo mete al row (''ctel' => $c['ctel'] ?? ''' línea 228), pero grep confirma que 'ctel' no aparece en ningún otro punto del archivo: la fila solo ofrece 'Editar' (línea 420). Los playbooks (playbook-data.php) tienen mensajes redactados con placeholder '[Nombre]' y canal 'WhatsApp', pero el modal los imprime como texto estático ('<div class="pb-msg-texto"><?= e($msg['texto']) ?></div>' línea 1427) sin botón copiar ni link wa.me.
- **Impacto:** El momento de máximo valor del producto (cliente caliente detectado) obliga al asesor a: abrir el playbook, leer el mensaje, reescribirlo a mano en WhatsApp sustituyendo [Nombre], y buscar el contacto. Cada fricción reduce la tasa de acción sobre señales calientes — exactamente lo que el Score/Seguimiento penaliza. Un vendedor de PyME con 8 calientes abiertos actúa sobre 2; con 1 clic actuaría sobre los 8.
- **Escenario que lo dispara (verificado):** Verificado leyendo el código: (a) el punto de inserción existe — ctel se carga en modules/radar/index.php:82 y se mete al row en :228 pero grep confirma que no se usa en ningún otro punto del archivo (dato muerto); la fila solo ofrece 'Editar' (:420) y el modal playbook imprime los mensajes como texto estático sin botón copiar ni wa.me (:1427), pese a que playbook-data.php trae mensajes con placeholder [Nombre] y canal 'WhatsApp' diseñados para enviarse. (b) Valor concreto y diario: cierra el loop detección→acción del feature insignia; el propio Termómetro penaliza Seguimiento (30%) cuando el asesor no actúa sobre calientes — el producto castiga la fricción que él mismo crea. (c) No duplica: los únicos wa.me existentes comparten la URL pública sin destinatario o son del cliente hacia la empresa; no existe 'contactar al cliente con mensaje de playbook'. El patrón wa.me/52+preg_replace ya existe en public/recibo.php:266, lo que valida el esfuerzo ~3h para el botón por fila. Matices: manejar ctel vacío, y el 'Enviar por WhatsApp' dentro del modal genérico requiere pasar contexto de fila (o limitarse a 'Copiar'). Severidad alto y no crítico porque nada está roto — es la mejora de activación de mayor apalancamiento del Radar, no un bug.
- **Fix:** En cada fila de bucket caliente agregar botón '💬' → https://wa.me/52{ctel limpio}?text={mensaje del playbook del bucket con [Nombre] sustituido por cnombre, urlencoded}. En el modal playbook (línea 1427) botón 'Copiar' y 'Enviar por WhatsApp' por mensaje. Esfuerzo ~3h.

### 🟠 ALTO — `modules/cotizaciones/ver.php:650`
**Compartir por WhatsApp manda solo la URL pelona: sin número del cliente ni mensaje profesional pre-armado**

- **Evidencia:** El overlay de compartir usa '<a href="https://wa.me/?text=<?= urlencode($url_publica) ?>"' — sin teléfono destino (el asesor debe buscar el contacto a mano) y el texto es únicamente la URL, aunque el sistema tiene cliente_telefono (se muestra en línea 377), nombre del cliente, folio, total y valida_hasta. En lista.php:519-531 las acciones son Editar/Ver/Copiar/Clonar/Suspender/Borrar — ni siquiera existe botón WhatsApp ahí.
- **Impacto:** El envío de la cotización es la primera impresión con el cliente final y el flujo diario #1. Hoy el cliente recibe un link seco sin contexto; un mensaje tipo 'Hola Juan, te comparto tu cotización COT-0231 por $45,200, vigente hasta el 15 de julio: {url}' se ve profesional, sube tasa de apertura (alimenta Activación del termómetro) y ahorra ~30s por envío.
- **Escenario que lo dispara (verificado):** Verificado leyendo el código: (a) ver.php:650 tiene exactamente el wa.me sin teléfono y con solo la URL como texto, mientras cliente_telefono/numero/total/valida_hasta ya están cargados en $cot (SELECT línea 19, teléfono mostrado en línea 377); lista.php carga ctel en línea 88 pero desk-actions (519-531) y exp-btns (463-476) no tienen botón WhatsApp. (b) Es el flujo diario #1 de envío de cotización en PyME mexicana — mensaje pre-armado con destino elimina fricción real en cada envío y profesionaliza la primera impresión. (c) No duplica nada: grep de wa.me confirma que ningún flujo de cotizaciones/ventas manda mensaje con contexto; el único con teléfono+texto es recibo.php:266 (dirección cliente→empresa), que además prueba que el patrón (wa.me/52 + preg_replace('/\D/','') + urlencode de mensaje) ya existe en el codebase y el estimado de ~2h es realista. Matices menores: fallback si el cliente no tiene teléfono y sanitizar números que ya traen prefijo 52. Severidad alto y no crítico porque nada está roto — existe workaround manual (copiar link) — pero es mejora barata de alto uso diario que toca la cara del producto ante el cliente final.
- **Fix:** Cambiar a wa.me/{52+telefono limpio}?text={mensaje con nombre, folio, total, vigencia y url}. Agregar el mismo botón '💬 Enviar' en lista.php desk-actions (línea 521) y exp-btns (línea 465) usando ctel ya cargado en la query (línea 88). ~2h.

### 🟠 ALTO — `core/PushNotification.php:65`
**Cero loop de retención: existe infra completa de push (APNs+WebPush) pero ningún resumen diario accionable — el único cron es de suscripciones**

- **Evidencia:** PushNotification::enviar_a_usuario() (línea 65) y enviar_a_empresa() (línea 103) están implementados y probados (se usan en abonos, quote_action, radar). Pero 'ls cron/' devuelve un solo archivo: procesar_suscripciones.php. No existe ningún digest diario, y el Radar solo se recalcula cuando alguien abre dashboard/radar o un cliente visita un slug (dashboard/index.php:380).
- **Impacto:** Si el asesor no abre la app, el producto no existe para él ese día. Un push a las 8am por asesor ('🔥 3 clientes calientes, 2 cotizaciones sin abrir 5 días, $18K por cobrar — abre tu Radar') convierte el Radar en hábito diario, que es exactamente lo que el termómetro mide (radar_view) y lo que sostiene la retención de un SaaS de $299-799/mes. Es la palanca de churn más barata disponible.
- **Escenario que lo dispara (verificado):** Verificado en código: (a) enviar_a_usuario() existe en core/PushNotification.php:65 con soporte APNs+WebPush, badges, logging y limpieza de tokens; tokens_usuario() en línea 55; cron/ contiene solo procesar_suscripciones.php — no hay digest. (b) No duplica: el único push de Radar (Radar.php:1518-1547) es por cambio de bucket, va a toda la empresa y solo dispara cuando recalcular() corre (apertura de dashboard/radar o visita de cliente al slug) — confirma el loop circular: sin actividad, cero notificaciones. (c) Esfuerzo ~4h realista: queries ya existen (ventas sin pago en dashboard, no_abiertas_5d en termómetro, radar_bucket en cotizaciones), notif_config() en Helpers ya gatea por evento, y procesar_suscripciones.php sirve de template de bootstrap. Matiz: los radar_bucket pueden estar rancios a las 8am — el cron debería invocar Radar::recalcular_empresa() (existe, línea 1550) antes de contar calientes, subiendo esfuerzo a ~5-6h y requiriendo cuidado con tiempo de ejecución multi-empresa. Valor de negocio directo: el termómetro pondera seguimiento del Radar al 30% del score; el push matutino crea el hábito que el producto mismo mide y es la palanca de churn más barata para un SaaS de $299-799/mes. Alto (no crítico) porque es retención/producto, no corrección de datos ni seguridad.
- **Fix:** Crear cron/resumen_diario.php (cron 8am America/Hermosillo): por asesor activo, contar buckets calientes, sin-abrir>3d y ventas sin pago, y mandar enviar_a_usuario() con deep link /radar. Respetar notif_config. ~4h.

### 🟠 ALTO — `modules/dashboard/index.php:327`
**Alerta 'Sin abrir' del dashboard no ofrece reenvío 1-clic: ni siquiera trae el teléfono del cliente**

- **Evidencia:** La query de sin_abrir_list (líneas 327-338) selecciona 'cl.nombre AS cliente_nombre' pero NO cl.telefono, y la fila renderizada (línea 1389) es solo '<a href="/cotizaciones/<?= (int)$sa['id'] ?>" class="alert-row">' hacia el editor. El playbook de no_abierta prescribe reenviar por WhatsApp hasta 3 veces (playbook-data.php:685-700).
- **Impacto:** 'Sin abrir 5 días' es la alerta con acción más obvia del sistema (reenviar el link), pero hoy exige: clic al editor → abrir overlay URL → WhatsApp genérico sin contacto. El costo de recuperar una cotización no abierta debería ser 1 tap; cada cotización no abierta que se recupera es dinero directo y mejora la Activación del asesor en el termómetro.
- **Escenario que lo dispara (verificado):** Verificado en código: (a) la query sin_abrir_list (dashboard/index.php:327-338) NO trae cl.telefono ni c.slug, y la fila (línea 1389) es solo un link al editor — punto de inserción exacto. (b) No duplica nada: el único share WhatsApp del sistema (cotizaciones/ver.php:650) es wa.me/?text= SIN teléfono (picker genérico), y el Radar selecciona ctel pero nunca lo renderiza (código muerto); no existe ningún wa.me/{telefono} en todo /modules. (c) Valor diario concreto: teléfono es campo obligatorio del cliente, WhatsApp es el canal prescrito por el propio playbook no_abierta y el dominante en el mercado PyME mexicano; recuperar cotizaciones sin abrir es dinero directo y alimenta Activación del termómetro. Esfuerzo ~1h realista con dos detalles omitidos pero triviales: normalizar teléfono a formato internacional (52...) y agregar c.slug + dominio de empresa para armar la URL pública. Es mejora de producto (no bug), de bajo esfuerzo y alta frecuencia de uso.
- **Fix:** Agregar cl.telefono a la query y un botón '💬 Reenviar' en la fila con wa.me/{tel}?text={mensaje del playbook no_abierta con nombre + url del slug}. ~1h.

### 🟡 MEDIO — `modules/radar/index.php:379`
**No existe 'ya lo contacté': el asesor ve la misma lista de calientes sin saber cuáles ya trabajó**

- **Evidencia:** Los únicos controles por fila son feedback de interés: '<button class="fb-btn..." onclick="radarFb({$cot_id_fb},'con_interes',this)">👍</button>' y 👎 (líneas 379-383). Grep de 'recordatorio|contactado|proximo_contacto' en modules/ solo da un hit irrelevante en bienvenida.php. El playbook de no_abierta dice 'Máximo 3 contactos sin respuesta' (playbook-data.php:700) pero el sistema no registra ningún contacto.
- **Impacto:** Un asesor con 15 cotizaciones en buckets calientes depende de su memoria para saber a quién ya le escribió hoy y a quién no — duplica mensajes (quema leads) o deja calientes sin tocar. Además el Score de Seguimiento solo puede medir feedback de interés, no la acción comercial real; con 'contactado' registrado, el termómetro tendría el dato que hoy le falta.
- **Escenario que lo dispara (verificado):** Verificado en código: (a) el punto de inserción existe tal cual — modules/radar/index.php:379-383 renderiza fb_html con solo 👍/👎 (radarFb con_interes/sin_interes) y el botón ❓; no hay ningún control de contacto. (b) No duplica nada: grep de contactado/recordatorio/seguimiento en modules/ da cero funcionalidad; el feedback radar_feedback registra evaluación de interés (UNIQUE por cotización, sin timestamp visible), no acción comercial; las notas internas son texto libre fuera de la lista del Radar. El playbook instruye 'máximo 3 contactos' pero el sistema no registra ninguno — inconsistencia producto-UI real. (c) La tabla cotizacion_log ya existe (evento, detalle, usuario_id, created_at) con 6+ archivos insertando en ella, por lo que 'sin migración' es correcto y hay patrón (radar_feedback.php) para calcar el endpoint; ~3h es realista (3-5h). Valor diario concreto para un vendedor de PyME con buckets calientes llenos (caso real: 79 cots en probable_cierre) y alimenta el score de Seguimiento del termómetro. Severidad media: es mejora de flujo, no defecto ni pérdida de datos, con workaround parcial; pero cierra el loop de acción del diferenciador del producto a costo mínimo.
- **Fix:** Botón '✆ Contactado' junto a 👍/👎 que inserte evento en cotizacion_log (evento='contactado', usuario_id) y muestre badge 'contactado hace 2h' en la fila (leer último log en la query principal). ~3h, sin migración.

### 🟡 MEDIO — `modules/radar/Radar.php:1536`
**Push del Radar se manda a TODA la empresa sin cliente ni monto, en vez de al vendedor asignado con contexto vendible**

- **Evidencia:** Al entrar a bucket caliente: 'PushNotification::enviar_a_empresa($empresa_id, 'radar_'.$r['bucket'], "Radar: {$label}", "{$ref} — {$label}", ['cotizacion_id'=>$cotizacion_id, 'url'=>'/radar'])'. $ref es solo numero/título; no incluye nombre del cliente ni monto, el deep link es el /radar genérico, y llega a todos los dispositivos de la empresa aunque la cotización tiene COALESCE(vendedor_id, usuario_id). enviar_a_usuario() existe (PushNotification.php:65) y ya se usa en soporte.
- **Impacto:** En empresas Business con 3+ asesores (OnTime: 3 sucursales) cada alerta caliente le suena a todos — el asesor aprende a ignorar pushes que no son suyos y la alerta pierde credibilidad, erosionando la promesa central 'te avisa cuando está listo'. Además 'Radar: Probable cierre / COT-0231' no dice quién ni cuánto: el asesor debe abrir la app para saber si le importa.
- **Escenario que lo dispara (verificado):** Verificado en código: Radar.php:1536 envía el push de bucket caliente con enviar_a_empresa() a todos los dispositivos, con body "{numero} — {label}" sin cliente ni monto y url genérica /radar; enviar_a_usuario() existe (PushNotification.php:65, ya probado en soporte) y nadie lo usa para Radar, así que no duplica nada. El SELECT de recalcular() ya trae vendedor_id (falta agregar usuario_id, cliente y total al query — esfuerzo ~2-3h, realista). El valor es concreto y diario: el contexto vendible en el body beneficia a todos los planes, y el targeting reduce fatiga de alertas en el segmento Business multi-usuario que es el diferenciador del plan de adquisición. Matices: las 3 sucursales OnTime son empresas separadas (el push no cruza sucursales, el ruido real es admin+asesores dentro de una empresa) y la mayoría de la base actual es de 1 vendedor donde ambos envíos son equivalentes; además el fix debe conservar el dedupe 24h y el toggle radar_alerta, y decidir si el admin/dueño conserva copia. Es mejora de producto sobre el corazón de la propuesta de valor, no un bug — prioridad media.
- **Fix:** Enviar a vendedor_id con enviar_a_usuario() (fallback a admin si no hay asignado), título 'María López está por cerrar 🎯', body con monto (rmoney) y bucket, url '/cotizaciones/{id}'. ~2h.

### 🟡 MEDIO — `modules/dashboard/index.php:302`
**El motivo de rechazo que escribe el cliente se consulta en el dashboard pero nunca se muestra**

- **Evidencia:** La query de rechazadas selecciona 'c.motivo_rechazo' (línea 302: "SELECT c.id, c.titulo, c.numero, c.rechazada_at, c.total, c.motivo_rechazo,..."), pero el render de la tarjeta (líneas 1335-1344) solo imprime cliente_nombre, titulo, numero, total y días — grep confirma que motivo_rechazo aparece una sola vez en el archivo (la query).
- **Impacto:** El motivo del rechazo ('muy caro', 'elegí otro proveedor', 'lo pospongo') es el dato más accionable de todo el embudo: distingue un lead muerto de uno recuperable con recotización o descuento. Hoy se captura, viaja hasta el dashboard y se tira. El asesor tiene que abrir cada cotización para verlo — la mayoría no lo hace.
- **Escenario que lo dispara (verificado):** Verificado en código: modules/dashboard/index.php:302 selecciona c.motivo_rechazo pero el render de rechazadas (líneas 1329-1344) no lo imprime — grep confirma que motivo_rechazo aparece una sola vez en el archivo y en ningún otro módulo de UI (solo se escribe en api/quote_action.php:266). El punto de inserción (alert-meta, línea 1338) existe y ya usa el patrón e(mb_substr(...)), por lo que el esfuerzo de 15 minutos es realista. Valor de negocio real: el motivo distingue lead muerto vs recuperable y hoy solo es visible abriendo cada cotización (vía cotizacion_log). Matiz que baja la severidad de alto a medio: el motivo SÍ llega al asesor por push (quote_action.php:287) y email (:299-304) al momento del rechazo — canales efímeros pero existentes y dependientes del toggle notif_config — por lo que la mejora aporta visibilidad persistente/recall, no información totalmente inaccesible. Además motivo_rechazo es opcional y puede venir vacío, lo que el guard propuesto maneja correctamente.
- **Fix:** En alert-meta de rechazadas (línea 1338) agregar el motivo truncado: '<?php if ($r['motivo_rechazo']): ?> · "'.e(mb_substr($r['motivo_rechazo'],0,60)).'"<?php endif ?>' con title completo. 15 minutos.

### 🟡 MEDIO — `modules/clientes/ver.php:53`
**Ficha de cliente (360) ciega al Radar: lista sus cotizaciones sin visitas, bucket ni última vista**

- **Evidencia:** La query de cotizaciones del cliente: "SELECT id, numero, titulo, total, estado, created_at FROM cotizaciones WHERE cliente_id = ?..." — no trae visitas, radar_bucket, ultima_vista_at ni vista_at, columnas que ya existen en la tabla y que lista.php sí explota (lista.php:81-82). Grep de 'quote_sessions|visitas' en clientes/ver.php: cero hits.
- **Impacto:** Cuando el asesor abre la ficha antes de llamar a un cliente, no ve la inteligencia que ya pagó: si el cliente abrió su última cotización 6 veces ayer o si nunca la abrió. La ficha es el lugar natural para preparar la llamada y hoy es una tabla contable. Diferenciador directo vs competencia (nadie da 'este cliente revisó el precio 3 veces esta semana' en la ficha).
- **Escenario que lo dispara (verificado):** Validado leyendo el código: la query en modules/clientes/ver.php:53-60 es exactamente la citada y no trae visitas, radar_bucket ni ultima_vista_at (grep con cero hits en el archivo), mientras lista.php:81-82 sí selecciona esas columnas y las renderiza con radar_badge() (lista.php:99, 419). No duplica funcionalidad existente: la ficha 360 hoy solo muestra estado plano vía badge_cot(). El valor es concreto y diario (preparar la llamada viendo si el cliente abrió/re-abrió su cotización, extiende el Radar —diferenciador núcleo del producto— a su superficie natural). Matiz: el esfuerzo declarado de ~2h es optimista porque radar_badge() no es helper compartido (vive dentro de lista.php y depende de ico(), habría que extraerla a Helpers.php) y hay que replicar el gating de estado activo/vencida; realista 3-4h. Y el dato ya es alcanzable buscando el cliente en /cotizaciones, por lo que es mejora de cobertura de superficie, no capacidad nueva — por eso severidad media, no alta.
- **Fix:** Agregar visitas, radar_bucket, ultima_vista_at al SELECT (línea 54) y renderizar el mismo radar_badge/👁 de lista.php en cada fila del historial. ~2h reusando radar_badge().

### 🟡 MEDIO — `modules/cotizaciones/ver.php:341`
**En el editor el bucket del Radar aparece como etiqueta muda: sin el '¿por qué?' ni playbook que sí existen en el módulo Radar**

- **Evidencia:** El header solo imprime el nombre crudo del bucket: '<span style="background:var(--purple-bg)..."><?= e(ucwords(str_replace('_', ' ', $cot['radar_bucket']))) ?></span>' (líneas 341-345). La explicación humana ya está construida — Radar::explicar_bucket() se usa en radar/index.php:375 con botón '❓ ¿Por qué aparece aquí?' — pero el editor, a donde llegan TODOS los clics desde dashboard, lista y alertas, no la ofrece.
- **Impacto:** El asesor Lite/Pro que nunca abre el módulo Radar ve 'Validando Precio' sin saber qué significa ni qué hacer — la confianza en el Radar se construye mostrando el porqué (el activo #1 del producto es esa confianza). Además el plan Lite depende 100% de estos badges (CLAUDE.md: Lite no tiene módulo Radar).
- **Escenario que lo dispara (verificado):** Verificado en código: (a) ver.php:341-345 imprime el bucket como badge mudo sin explicación ni interacción — punto de inserción exacto; (b) Radar::explicar_bucket() existe (Radar.php:1635) y solo se usa en radar/index.php:375, el editor no lo ofrece, por lo que no duplica nada; radar_senales es columna JSON de cotizaciones y ya llega en $cot vía SELECT c.* (solo falta json_decode), el estimado de ~2h es realista copiando el patrón del botón ❓ de radar/index.php; (c) valor diario concreto: el editor es la superficie principal de clics y el asesor ve el bucket sin saber qué significa. Matiz que baja la severidad: el plan Lite (argumento central del impacto) aún no está implementado, y en Pro/Business el módulo Radar sí da la explicación a un clic — es fricción de descubrimiento, no capacidad ausente ni bug.
- **Fix:** Hacer el badge clickable: popover con Radar::explicar_bucket($senales) + link '📖 Ver playbook' (a /radar o /ayuda/buckets en Lite). radar_senales ya viene en $cot. ~2h.

### 🟡 MEDIO — `core/layout.php:594`
**Sin botón '+' de creación en el bottom nav móvil: la acción #1 del vendedor en campo queda a 3 taps**

- **Evidencia:** El bottom nav tiene 5 items de navegación pura: '<a href="/dashboard"...Inicio</a> <a href="/cotizaciones"...Cotizaciones</a> <a href="/ventas"...Ventas</a> ... <button ...Más</button>' (líneas 594-604). Crear cotización en móvil exige: tap Cotizaciones → scroll al header → tap 'Nueva cotización' (lista.php:320).
- **Impacto:** El usuario objetivo es el vendedor mexicano de PyME con el teléfono en la mano frente al cliente. Cotizar en el momento (mueblería, inmobiliaria, seguros) es el diferencial de la app móvil que se vende en el plan Pro. Cada tap extra en ese momento es una cotización que se hace 'al rato' o nunca — menos volumen, menos datos para el Radar.
- **Escenario que lo dispara (verificado):** Validado en código: el bottom nav (core/layout.php:594-604) tiene 5 items de navegación pura sin acción de creación; no existe FAB de creación ni acción rápida en dashboard (el único FAB es el chat de soporte czs-fab); el gate Auth::puede('crear_cotizaciones') ya existe y se reusa de lista.php:319; el límite Free se aplica server-side en crear.php así que no abre bypass. Esfuerzo 1-2h realista (solo HTML/CSS en layout.php, cuidando la variante Lite del nav). Matiz: son 2 taps, no 3 (el botón 'Nueva cotización' está en el header sin scroll), y desde la ficha de cliente ya hay atajo con cliente_id precargado — por eso no es alto: es mejora UX legítima y barata para la acción diaria #1 del vendedor móvil (diferencial del plan Pro), no un bug ni un bloqueo.
- **Fix:** FAB central '+' en el bottom nav (patrón Instagram/WhatsApp Business) → /cotizaciones/nueva, visible si Auth::puede('crear_cotizaciones'). ~1-2h de CSS+HTML en layout.php.

### 🟡 MEDIO — `modules/cotizaciones/ver.php:609`
**Patrón horario de apertura del cliente: dato ya almacenado en quote_sessions y jamás explotado (0 usos de HOUR() en todo el repo)**

- **Evidencia:** quote_sessions guarda created_at de cada visita real del cliente (ver.php:73-83 las lista una por una), pero grep de 'HOUR(' en todo /home/user/cotizacloud devuelve cero resultados: nadie agrega por hora del día. Los playbooks dicen QUÉ mandar y por qué canal, pero nunca CUÁNDO — y el dato para saberlo ya está en la BD.
- **Impacto:** Un mensaje de WhatsApp enviado en la ventana en que el cliente suele revisar la cotización (ej. 20-22h) tiene tasa de respuesta muy superior a uno a las 11am. 'Este cliente suele revisar tus cotizaciones entre 8pm y 10pm' es una línea que ningún competidor de cotizaciones ofrece y refuerza la percepción de inteligencia del Radar — el argumento de venta de la demo.
- **Escenario que lo dispara (verificado):** Verificado en código: (a) el punto de inserción existe — el panel "Historial de visitas" está en ver.php:588-612 y la línea 609 es la nota al pie donde encaja el patrón horario; cliente_id ya está en contexto. (c) No duplica nada — grep de HOUR( en todo el repo da 0 resultados y ninguna feature (playbooks, DiagnosticoTips, Radar, termómetro) usa la dimensión hora del día. (b) El valor es real y diferenciador para la demo del Radar, aunque intermitente: con el gate de ≥3 visitas + franja >40% solo aplicará a una fracción de clientes. Dos correcciones obligatorias que el proponente omitió: (1) TIMEZONE — DB.php:34-37 sincroniza MySQL al tz del servidor (UTC-4) y está documentado que difiere 3h de Hermosillo (UTC-7); sin conversión por empresa la franja mostrada estaría corrida 3 horas y dañaría la credibilidad del Radar; no existe columna de tz por empresa. (2) La query debe replicar el filtro anti-ghost del propio archivo (scroll_max>0 OR visible_ms>=2000, ver.php:78-79) y proteger cliente_id NULL. Esfuerzo realista ~4h, no 3h. Severidad media: barato, cero riesgo, buen argumento de venta, pero no es bug ni fuga de ingresos y su uso diario es intermitente.
- **Fix:** Bajo el historial de visitas (línea 609): query GROUP BY HOUR(created_at) sobre quote_sessions es_interno=0 del cliente_id (todas sus cots); si hay ≥3 visitas y una franja concentra >40%, mostrar '🕐 Suele revisar entre Xh y Yh'. ~3h.

### 🟡 MEDIO — `modules/cotizaciones/ver.php:690`
**El dialog 'Asignar cliente' del editor no permite crear cliente nuevo, a diferencia del builder de nueva cotización**

- **Evidencia:** En nueva.php existe tab '<button class="sh-tab" id="tab-new" onclick="switchClientTab('new')">Nuevo cliente</button>' (nueva.php:706) con crearClienteNuevo() (nueva.php:1096). El clientDialog de ver.php (líneas 690-703) solo tiene input de búsqueda y lista — grep de 'crearCliente|Nuevo cliente' en ver.php: cero hits.
- **Impacto:** Flujo real frecuente: el asesor clona una cotización (feature existente en lista.php:590) o retoma un borrador para un cliente NUEVO — desde el editor no puede darlo de alta; tiene que ir a Clientes, crearlo, volver y buscarlo. Tres cambios de contexto en móvil para algo que el otro builder ya resuelve inline.
- **Escenario que lo dispara (verificado):** Verificado en código: ver.php:690-703 tiene el clientDialog solo con búsqueda+lista (cero hits de crearCliente/Nuevo cliente), mientras nueva.php:706/1096 sí tiene el tab y crearClienteNuevo() portable (POST /clientes con CSRF; ver.php ya expone CSRF_TOKEN y CLIENTES). El flujo de impacto es real: clonarCot existe (lista.php:590) y el clon copia el cliente original, forzando al asesor a salir del editor, crear el cliente en otro módulo y volver — fricción concreta en un producto mobile-first. No duplica nada existente. Correcciones menores: el endpoint es POST /clientes (no /clientes/crear) y la adaptación no es copy-paste puro (nueva.php usa bottom-sheet div, ver.php usa <dialog> nativo), pero el estimado ~1-2h es realista. Severidad media, no alta: hay workaround y no bloquea ventas — es paridad de UX con retorno diario pero incremental.
- **Fix:** Portar el tab 'Nuevo cliente' + crearClienteNuevo() de nueva.php:706/1096 al clientDialog de ver.php (mismo endpoint /clientes/crear). ~1h de copy-adapt.
